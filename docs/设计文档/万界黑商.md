# 万界黑商 — NPC 集市交易系统设计

> **版本**：v0.10.1（服务端内存锁 + 错误文本修正）
> **日期**：2026-05-04
> **定位**：有限的 NPC 做市商交易，非玩家间直接交易
> **前置依赖**：C4 Money.Cost 验证 ✅、C5 BatchCommit 验证 ✅（无灵韵迁移依赖）

---

## 1. 系统概述

「万界黑商」是一个游走于各界的神秘商人 NPC。他以固定价格、限量收购的方式向玩家收购神器碎片，再以加价出售给其他有需要的玩家。

### 1.1 核心规则

| 规则 | 说明 |
|------|------|
| **NPC 做市** | 所有交易通过黑商 NPC，玩家之间不直接交易 |
| **固定价格** | 每种商品有独立的收购价和出售价，**不随库存变动** |
| **限量收购** | 每种商品有库存上限（碎片类 `max_stock = 5`，材料类 `max_stock = 5~10`），满仓不收 |
| **零初始库存** | 黑商初始没有任何商品，所有货物来自玩家出售 |
| **不刷新库存** | 无每日/每周重置，库存纯粹跟随交易实时变动 |
| **仙石交易** | 所有交易使用专用货币「仙石」，玩家需先用灵韵兑换仙石 |
| **开始即可见** | 图标从游戏开始即显示，境界不足时可浏览商品但不可操作（筑基初期解锁操作） |
| **背包直接操作** | 物品持有量从背包（`saveData.backpack`）直接计数和扣减，**无 Money 域物品迁移** |

### 1.2 交易流向

```
                         兑换
玩家灵韵 ──100灵韵:1仙石──→ 仙石余额
                              │
                              ↓
玩家A（有多余碎片）──卖给黑商──→ 黑商库存 ──买走──→ 玩家B（急需碎片）
                   收购价(仙石)              出售价(仙石)

仙石余额 ──1仙石:50灵韵──→ 玩家灵韵（反向兑换，50%折损）
```

### 1.3 仙石货币

| 属性 | 说明 |
|------|------|
| **名称** | 仙石 |
| **用途** | 万界黑商专用货币，不可在其他系统使用、出售或丢弃 |
| **正向兑换** | 100 灵韵 → 1 仙石（整数兑换，不足 100 不可兑换） |
| **反向兑换** | 1 仙石 → 50 灵韵（50% 折损，灵韵净回收） |
| **数据域** | Money 域原生货币，key = `xianshi`，天然支持 `Cost/Add/Get` |

**经济安全性**：

- **正向兑换（灵韵→仙石）**：灵韵在存档中先扣除并 `WriteSaveBack`，成功后 `BatchCommit:MoneyAdd(xianshi)`。崩溃场景：灵韵扣了但仙石未加 → 写 `WAL_SELL_KEY` 补偿，下次登录 `CheckWALOnLogin` 自动补发仙石
- **反向兑换（仙石→灵韵）**：`BatchCommit` 原子执行 `MoneyCost(xianshi)` + `ListAdd(WAL_KEY, lingYunGain)`。成功后通知客户端加灵韵，再清除 WAL。崩溃场景：仙石扣了但灵韵未加 → WAL 条目保留，下次登录 `CheckWALOnLogin` 自动补偿灵韵到存档
- **套利不可能**：100 灵韵 → 1 仙石 → 50 灵韵，往返损失 50%

### 1.4 UI 入口

- **入口按钮**：小地图容器（`minimapContainer`）下方，72×90 像素，显示 NPC 图标（`Textures/icon_black_merchant.png`）+ "黑市" 文字
- **NPC 入口**：天机阁地图区域 NPC "大黑无天"（`interactType = "black_merchant"`），与 NPCDialog 系统集成，对话后打开交易面板
- **可见性**：**游戏开始即可见**，无需任何前置条件
- **操作权限**：
  - 境界 < 筑基初期（`order < 4`）：点击可打开交易面板，展示所有商品（名称、价格、库存），但所有操作按钮（买/卖/兑换）**置灰不可点击**，面板顶部显示提示：`"你修为太低了，筑基初期修士才可以入内"`
  - 境界 ≥ 筑基初期（`order >= 4`）：正常交易
- **服务端校验**：无论客户端状态如何，所有 C2S 请求（兑换/买/卖）均校验境界 ≥ 筑基初期，不满足则拒绝

---

## 2. 定价模型

### 2.1 固定价格

所有商品采用**固定价格**，不随库存变动，不波动：

| 参数 | 含义 |
|------|------|
| `buy_price` | 收购价（黑商从玩家买入，玩家获得仙石） |
| `sell_price` | 出售价（黑商卖给玩家，玩家花费仙石） |

**黑商永远赚差价**：`sell_price = buy_price × 2`。

### 2.2 价格表

#### 上宝逊金钯碎片（三章神器，8 种）

> `rake_fragment_9`（残片·玖，朱老二 100 万金购买）**不纳入黑商**——该碎片有稳定的金币获取途径，无需交易流通。

| consumableId | 名称 | 收购价(仙石) | 出售价(仙石) | 库存上限 | 备注 |
|-------------|------|------------|------------|---------|------|
| `rake_fragment_1` | 残片·壹 | 10 | 20 | 5 | 枯木妖王 |
| `rake_fragment_2` | 残片·贰 | 10 | 20 | 5 | 岩蟾妖王 |
| `rake_fragment_3` | 残片·叁 | 10 | 20 | 5 | 苍狼妖王 |
| `rake_fragment_4` | 残片·肆 | 10 | 20 | 5 | 赤甲妖王 |
| `rake_fragment_5` | 残片·伍 | 10 | 20 | 5 | 蛇骨妖王 |
| `rake_fragment_6` | 残片·陆 | 10 | 20 | 5 | 烈焰狮王 |
| `rake_fragment_7` | 残片·柒 | 10 | 20 | 5 | 蜃妖王 |
| `rake_fragment_8` | 残片·捌 | **15** | **30** | 5 | **黄天大圣（守关BOSS，`isBoss = true`）** |

#### 文王八卦盘碎片（四章神器，8 种）

| consumableId | 名称 | 收购价(仙石) | 出售价(仙石) | 库存上限 | 备注 |
|-------------|------|------------|------------|---------|------|
| `bagua_fragment_kan` | 卦象·坎 | 15 | 30 | 5 | 沈渊衣 |
| `bagua_fragment_gen` | 卦象·艮 | 15 | 30 | 5 | 岩不动 |
| `bagua_fragment_zhen` | 卦象·震 | 15 | 30 | 5 | 雷惊蛰 |
| `bagua_fragment_xun` | 卦象·巽 | 15 | 30 | 5 | 风无痕 |
| `bagua_fragment_li` | 卦象·离 | 15 | 30 | 5 | 炎若晦 |
| `bagua_fragment_kun` | 卦象·坤 | 15 | 30 | 5 | 厚德生 |
| `bagua_fragment_dui` | 卦象·兑 | 15 | 30 | 5 | 泽归墟 |
| `bagua_fragment_qian` | 卦象·乾 | **20** | **40** | 5 | **司空正阳（守关BOSS，`isBoss = true`）** |

#### 天帝剑痕碎片（五章神器，当前 3 种可交易）

> 碎片·肆~玖（`tiandi_fragment_4~9`）对应星魂/天裂战场，**暂无掉落来源，不纳入黑商**，待后续区域实现后追加。

| consumableId | 名称 | 收购价(仙石) | 出售价(仙石) | 库存上限 | 备注 |
|-------------|------|------------|------------|---------|------|
| `tiandi_fragment_1` | 剑痕·壹 | 25 | 50 | 5 | 仙劫战场·域外邪魔 |
| `tiandi_fragment_2` | 剑痕·贰 | 25 | 50 | 5 | 仙劫战场·域外邪魔 |
| `tiandi_fragment_3` | 剑痕·叁 | 25 | 50 | 5 | 仙劫战场·域外邪魔 |

#### 附灵玉（v0.9 新增，4 种，对应四大 T9 套装）

| consumableId | 名称 | 收购价(仙石) | 出售价(仙石) | 库存上限 | 备注 |
|-------------|------|------------|------------|---------|------|
| `lingyu_xuesha_lv1` | 血煞附灵玉 | 20 | 40 | 5 | 附灵师·萃取产出 |
| `lingyu_qingyun_lv1` | 青云附灵玉 | 20 | 40 | 5 | 附灵师·萃取产出 |
| `lingyu_fengmo_lv1` | 封魔附灵玉 | 20 | 40 | 5 | 附灵师·萃取产出 |
| `lingyu_haoqi_lv1` | 浩气附灵玉 | 20 | 40 | 5 | 附灵师·萃取产出 |

#### 炼丹材料（14 种：基础 4 + 中洲精华 5 + 龙神材料 4 + 金砖 1）

| consumableId | 名称 | 收购价(仙石) | 出售价(仙石) | 库存上限 | 分类内排序 | 备注 |
|-------------|------|------------|------------|---------|----------|------|
| `tiger_bone` | 虎骨 | 1 | 2 | 10 | 1 | 虎王掉落 |
| `snake_fruit` | 灵蛇果 | 2 | 4 | 10 | 2 | 蛇妖·碧鳞掉落 |
| `diamond_wood` | 金刚木 | 3 | 6 | 10 | 3 | 乌天南/乌地北掉落 |
| `wind_eroded_grass` | 风蚀草 | 4 | 8 | 10 | 4 | 沙漠九寨怪物掉落 |
| `lipo_essence` | 力魄精华 | 5 | 10 | 10 | 5 | 中洲·阵营挑战掉落 |
| `panshi_essence` | 磐石精华 | 5 | 10 | 10 | 6 | 中洲·阵营挑战掉落 |
| `yuanling_essence` | 元灵精华 | 5 | 10 | 10 | 7 | 中洲·阵营挑战掉落 |
| `shihun_essence` | 噬魂精华 | 5 | 10 | 10 | 8 | 中洲·阵营挑战掉落 |
| `lingxi_essence` | 灵息精华 | 5 | 10 | 10 | 9 | 中洲·阵营挑战掉落 |
| `dragon_scale_ice` | 冰龙鳞 | 15 | 30 | 5 | 10 | 冰龙掉落 |
| `dragon_scale_abyss` | 渊龙鳞 | 15 | 30 | 5 | 11 | 渊龙掉落 |
| `dragon_scale_fire` | 火龙鳞 | 15 | 30 | 5 | 12 | 火龙掉落 |
| `dragon_scale_sand` | 沙龙鳞 | 15 | 30 | 5 | 13 | 沙龙掉落 |
| `gold_brick` | 金砖 | 2 | 5 | 10 | 14 | — |

> **注意**：金砖的出售价为 5（非 4），是唯一不严格遵循 `sell_price = buy_price × 2` 的商品（`sell_price = buy_price × 2.5`）。

#### 特殊装备（v0.10 新增，4 种帝尊/四龙戒指）

> 装备类商品与消耗品不同：**卖出时接受任何附魔/洗练状态**的装备，**买入时始终为初始状态**（通过 `LootSystem.CreateSpecialEquipment` 创建）。每件装备占背包 1 格，不可堆叠。

| equipId | 名称 | 收购价(仙石) | 出售价(仙石) | 库存上限 | 掉落来源 | 掉率 |
|---------|------|------------|------------|---------|---------|------|
| `dizun_ring_ch1` | 帝尊壹戒 | 2 | 4 | 5 | 虎王（初章） | 3% |
| `dizun_ring_ch2` | 帝尊贰戒 | 4 | 8 | 5 | 乌万仇/乌万海（贰章） | 2% |
| `dizun_ring_ch3` | 帝尊叁戒 | 8 | 16 | 5 | 沙万里/狮王/蜃妖王（叁章） | — |
| `silong_ring_ch4` | 四龙戒指 | 15 | 30 | 5 | 四龙（肆章） | — |

**装备买卖规则**：

| 规则 | 说明 |
|------|------|
| **卖出** | 从背包移除匹配 `equipId` 的装备，**不论附魔、洗练属性**（`RemoveEquipmentFromBackpack`） |
| **买入** | 通过 `LootSystem.CreateSpecialEquipment(equipId)` 创建**全新初始状态**装备写入背包 |
| **库存 key** | `bm_stock_{equipId}`（与消耗品格式一致） |
| **持有量计数** | `BackpackUtils.CountEquipmentItem(backpack, equipId)` 按 equipId 匹配非消耗品类 |

#### 五一活动信物（v0.9 新增，4 种限时道具）

> 福袋不可交易。

| consumableId | 名称 | 收购价(仙石) | 出售价(仙石) | 库存上限 | 备注 |
|-------------|------|------------|------------|---------|------|
| `mayday_wu` | 信物·武 | 3 | 6 | 5 | 五一活动限时 |
| `mayday_yi` | 信物·毅 | 3 | 6 | 5 | 五一活动限时 |
| `mayday_kuai` | 信物·快 | 3 | 6 | 5 | 五一活动限时 |
| `mayday_le` | 信物·乐 | 3 | 6 | 5 | 五一活动限时 |

#### 定价逻辑说明

| 碎片类型 | 普通碎片 | BOSS 碎片 | 溢价倍率 |
|---------|---------|----------|---------|
| 钉耙碎片（三章） | 10 / 20 仙石 | 15 / 30 仙石 | 1.5× |
| 八卦碎片（四章） | 15 / 30 仙石 | 20 / 40 仙石 | 1.33× |
| 天帝碎片（五章） | 25 / 50 仙石 | — | — |
| 附灵玉 | 20 / 40 仙石 | — | — |
| 龙神材料 | 15 / 30 仙石 | — | — |
| 帝尊戒指（初~叁章） | 2~8 / 4~16 仙石 | — | 章节递增 |
| 四龙戒指（肆章） | 15 / 30 仙石 | — | — |

### 2.3 商品汇总

| 类别 | 分类 key | 种类数 | 收购价范围 | 出售价范围 | 库存上限 |
|------|----------|--------|-----------|-----------|---------|
| 钉耙碎片（普通） | `rake` | 7 | 10 | 20 | 5 |
| 钉耙碎片·捌（BOSS） | `rake` | 1 | 15 | 30 | 5 |
| 八卦碎片（普通） | `bagua` | 7 | 15 | 30 | 5 |
| 八卦碎片·乾（BOSS） | `bagua` | 1 | 20 | 40 | 5 |
| 天帝碎片（壹~叁） | `tiandi` | 3 | 25 | 50 | 5 |
| 附灵玉 | `lingyu` | 4 | 20 | 40 | 5 |
| 特殊装备（帝尊/四龙戒指） | `special_equip` | 4 | 2~15 | 4~30 | 5 |
| 炼丹材料（基础 4 种） | `material` | 4 | 1~4 | 2~8 | 10 |
| 炼丹材料（中洲精华 5 种） | `material` | 5 | 5 | 10 | 10 |
| 龙神材料（4 种） | `material` | 4 | 15 | 30 | 5 |
| 金砖 | `material` | 1 | 2 | 5 | 10 |
| 五一活动信物 | `event` | 4 | 3 | 6 | 5 |
| **合计** | **8 分类** | **45 种** | | | |

> 天帝碎片·肆~玖（星魂/天裂战场）待区域实装后追加。特殊装备品类会随后续版本扩充更多装备。

### 2.4 经济效果

**所有套利路径均不可盈利**：

| 套利路径 | 计算 | 结果 |
|---------|------|------|
| 买入-转卖同种 | 花 20 买 → 卖回得 10 | 亏损 10 仙石 |
| 灵韵↔仙石往返 | 100灵韵→1仙石→50灵韵 | 亏损 50% |
| 跨品套利（低→高） | 花 20 买钉耙 → 无法卖成八卦 | 不同品类不互通 |
| 买碎片→仙石→灵韵 | 花 20 买碎片 → 卖回 10 仙石 → 500 灵韵 | 净亏 1000 灵韵 |

---

## 3. serverCloud 数据模型

### 3.1 方案：仙石 → Money 域，物品 → 背包直操

仙石天然是 Money 域货币。**物品持有量不使用 Money 域**，而是直接从存档背包（`saveData.backpack`）中计数和扣减，通过 `BackpackUtils` 共享模块操作。

买入操作：`BatchCommit(扣仙石 + 扣库存 + 写背包WAL)` → 背包写入 → 清除 WAL。
卖出操作：背包扣除 → `WriteSaveBack` → `BatchCommit(加仙石 + 加库存)`。

**v0.10 装备类扩展**：装备类商品（`itemType = "equipment"`）使用相同的 Money 域库存 key 格式，但背包操作使用专用的 `BackpackUtils.CountEquipmentItem / AddEquipmentToBackpack / RemoveEquipmentFromBackpack` 函数，按 `equipId` 匹配。Handler 内部通过 `CountItemHeld()` 统一分派。

### 3.2 键设计

| 域 | userId | key 模式 | 值 | 用途 |
|----|--------|---------|-----|------|
| Money | `playerId` | `xianshi` | 整数 | 玩家仙石余额 |
| Money | `SYSTEM_UID(0)` | `bm_stock_{consumableId}` | 整数 | 黑商当前库存 |
| List | `SYSTEM_UID(0)` | `bm_pub_trade` | JSON 字符串 | 公共买卖记录（含玩家名） |
| List | `playerId` | `bm_trade_log` | JSON 字符串 | 个人兑换记录 |
| List | `playerId` | `bm_wal` | JSON | WAL：灵韵补偿（仙石→灵韵崩溃） |
| List | `playerId` | `bm_wal_bp` | JSON | WAL：背包物品补偿（购买后背包未写入） |
| List | `playerId` | `bm_wal_sell` | JSON | WAL：仙石补偿（出售背包已扣但仙石未到） |

**与 v0.8 文档的关键差异**：
- ~~`mat_{consumableId}` Money 域键~~：**已废弃**。物品持有量直接从背包读取（`BackpackUtils.CountBackpackItem`），不存在 Money 域物品键
- WAL 从 1 种扩展为 3 种（`bm_wal` / `bm_wal_bp` / `bm_wal_sell`），覆盖所有崩溃场景

**无交易限额**：个人购买、出售不限次数。库存上限（`max_stock`）是唯一的数量约束。

**不需要的数据**：
- 不需要价格存储（价格固定，写在 `BlackMerchantConfig.lua` 中）
- 不需要物品 Money 域键（从背包直接计数）
- 不需要交易计数器（无交易次数限制）
- 不需要 NPC 钱包（黑商仙石无限，不做收支平衡）

### 3.3 数据规模

| 指标 | 数量 | 说明 |
|------|------|------|
| 商品种类 | 45 | 每种占 1 个 SYSTEM Money key（含 4 种装备） |
| SYSTEM Money key | 45 | `bm_stock_{id}`（消耗品用 consumableId，装备用 equipId） |
| 玩家 Money key | 1 | `xianshi`（物品不走 Money） |
| BatchCommit 操作数/笔交易 | 2~3 | 买入: `Cost xianshi + Cost stock + ListAdd WAL`；卖出: `Add xianshi + Add stock` |

### 3.4 交易记录（List 域，公共+个人双轨）

每笔交易完成后，服务端向 List 域追加流水记录。**买/卖操作写公共记录**（所有玩家可见），**兑换操作写个人记录**（仅自己可见）。

#### 键设计

| 域 | userId | key | 用途 | 上限 |
|----|--------|-----|------|------|
| List | `SYSTEM_UID(0)` | `bm_pub_trade` | 公共买卖记录（含玩家角色名 `pn`） | 50 条 |
| List | `playerId` | `bm_trade_log` | 个人兑换记录 | 50 条 |

#### 单条记录结构

**公共记录**（买/卖）：

```lua
{
    t  = 1712745600,           -- 时间戳（服务端 Unix 秒）
    a  = "sell",               -- 动作："buy" / "sell"
    id = "rake_fragment_1",    -- 商品 ID
    n  = 1,                    -- 数量
    p  = 10,                   -- 单价（仙石）
    xs = 60,                   -- 交易后仙石余额
    pn = "修仙者",              -- 玩家角色名
}
```

**个人记录**（兑换）：

```lua
{
    t  = 1712745600,           -- 时间戳
    a  = "exchange_buy",       -- 动作："exchange_buy" / "exchange_sell"
    id = "xianshi",            -- 固定为 "xianshi"
    n  = 5,                    -- 数量
    p  = 100,                  -- 汇率
    xs = 55,                   -- 交易后仙石余额
}
```

#### 记录策略

| 策略 | 值 | 说明 |
|------|-----|------|
| 写入方式 | `TradeLogger.LogPublic` / `TradeLogger.LogPersonal` | 公共/个人分别调用 |
| 公共上限 | 50 条 | `MAX_TRADE_RECORDS = 50` |
| 个人上限 | 50 条 | `MAX_PERSONAL_RECORDS = 50` |
| FIFO 淘汰 | 按时间戳升序排序后删除最旧 | `TrimList()` 在每次 Add 后执行 |
| 查询合并 | HandleHistory 并行查公共+个人两个 list | 合并后按时间倒序，总量上限 100 条 |

---

## 4. 协议设计

### 4.1 C2S 协议

| 协议常量 | 实际事件 ID | 字段 | 说明 |
|---------|-----------|------|------|
| `C2S_BlackMerchantQuery` | `C2S_BMQuery` | — | 查询全部商品库存 + 玩家仙石余额 + 背包持有量 |
| `C2S_BlackMerchantBuy` | `C2S_BMBuy` | `consumableId: string, amount: int` | 从黑商购买物品（花仙石） |
| `C2S_BlackMerchantSell` | `C2S_BMSell` | `consumableId: string, amount: int` | 向黑商出售物品（得仙石） |
| `C2S_BlackMerchantExchange` | `C2S_BMExchange` | `direction: "buy"/"sell", amount: int` | 仙石兑换：`"buy"` = 灵韵→仙石，`"sell"` = 仙石→灵韵 |
| `C2S_BlackMerchantHistory` | `C2S_BMHistory` | — | 查询交易记录（公共+个人合并） |

### 4.2 S2C 协议

| 协议常量 | 实际事件 ID | 字段 | 说明 |
|---------|-----------|------|------|
| `S2C_BlackMerchantData` | `S2C_BMData` | `ok, realm_ok, xianshi, items(JSON), walCompensation?` | 全部商品列表 + 仙石余额 + 是否达到境界 + WAL补偿灵韵 |
| `S2C_BlackMerchantResult` | `S2C_BMResult` | `ok, action, id, amount, total, newStock, newXianshi, newHeld, reason?` | 买/卖交易结果 |
| `S2C_BlackMerchantExchangeResult` | `S2C_BMExchangeResult` | `ok, direction, amount, newXianshi, lingYunDelta, reason?` | 兑换结果 |
| `S2C_BlackMerchantHistory` | `S2C_BMHistory` | `ok, records(JSON)` | 交易记录列表（公共+个人合并，按时间倒序，≤100 条） |

**S2C_BlackMerchantData 响应详情**：

```lua
-- VariantMap 字段：
ok = true,                -- bool
realm_ok = true,          -- bool: 是否达到筑基初期
xianshi = 50,             -- int: 玩家仙石余额
items = '{"rake_fragment_1":{"stock":3,"buy_price":10,"sell_price":20,"held":2},...}',  -- JSON 字符串
walCompensation = 150,    -- int|nil: 灵韵 WAL 补偿量（有时附带）
```

**items JSON 中每个商品**：

```lua
{
    stock = 3,        -- 黑商库存（从 SYSTEM Money 域读取）
    buy_price = 10,   -- 收购价（从 BlackMerchantConfig 读取）
    sell_price = 20,  -- 出售价（从 BlackMerchantConfig 读取）
    held = 2,         -- 玩家持有量（从 saveData.backpack 计数，非 Money 域）
}
```

### 4.3 完整交易时序

```
┌──────────────────────────────────────────────────────────────────┐
│                      打开黑商界面                                  │
├──────────────────────────────────────────────────────────────────┤
│  C2S_BMQuery → 服务端：                                           │
│    1) EnsureRealmCached(从存档读境界 order)                         │
│    2) 并行读取:                                                   │
│       - serverCloud.money:Get(userId) → xianshi                   │
│       - GetSystemStockCached(SYSTEM_UID) → 41种库存(10秒TTL缓存)   │
│       - serverCloud.list:Get(WAL) → 检查灵韵WAL补偿               │
│    3) CountBackpackItem(backpack, id) → 每种商品持有量              │
│    4) 从 BMConfig.ITEMS 填充固定价格                               │
│  ← S2C_BMData { ok, realm_ok, xianshi, items(JSON), walComp? }    │
│  启动 30 秒轮询定时器                                              │
│                                                                  │
│  if !realm_ok:                                                   │
│      显示所有商品（只读），顶部提示：                                │
│      "你修为太低了，筑基初期修士才可以入内"                            │
│      所有按钮置灰，return                                          │
├──────────────────────────────────────────────────────────────────┤
│                   玩家点击"兑换仙石"（灵韵→仙石）                   │
├──────────────────────────────────────────────────────────────────┤
│  客户端：弹出数量选择（+1/-1/+5/-5/MAX）                            │
│  客户端：确认后发 C2S_BMExchange { direction="buy", amount=5 }      │
│  → 服务端：校验境界 → 读存档验灵韵 >= 500                          │
│    → 扣灵韵 WriteSaveBack → BatchCommit MoneyAdd(xianshi,5)        │
│    → 失败则写 WAL_SELL_KEY 补偿                                    │
│  ← S2C_BMExchangeResult { ok=true, newXianshi=55, lingYunDelta=-500}│
│  客户端：扣灵韵、更新仙石显示                                       │
├──────────────────────────────────────────────────────────────────┤
│                      玩家点击"出售"碎片                             │
├──────────────────────────────────────────────────────────────────┤
│  客户端：弹出确认（3秒倒计时）                                      │
│  C2S_BMSell { consumableId="rake_fragment_1", amount=1 }          │
│  → 服务端：校验境界 → 读存档验背包持有量 → 验库存上限                │
│    → RemoveFromBackpack → WriteSaveBack                           │
│    → BatchCommit: MoneyAdd(xianshi,10) + MoneyAdd(stock,1)         │
│    → 失败则写 WAL_SELL_KEY 补偿仙石                                │
│  ← S2C_BMResult { ok=true, action="sell", total=10, ... }         │
│  客户端：更新库存和仙石显示                                         │
├──────────────────────────────────────────────────────────────────┤
│                      玩家点击"购买"碎片                             │
├──────────────────────────────────────────────────────────────────┤
│  客户端：弹出确认（3秒倒计时）                                      │
│  C2S_BMBuy { consumableId="rake_fragment_1", amount=1 }           │
│  → 服务端：校验境界 → 读 Money 验仙石 → 读 SYSTEM 验库存            │
│    → BatchCommit: MoneyCost(xianshi,20) + MoneyCost(stock,1)      │
│      + ListAdd(WAL_BP_KEY, 物品信息)                               │
│    → 成功后读存档 → AddToBackpack → WriteSaveBack → 清WAL          │
│  ← S2C_BMResult { ok=true, action="buy", total=20, ... }         │
│  客户端：更新库存和仙石显示                                         │
├──────────────────────────────────────────────────────────────────┤
│                      关闭黑商界面                                  │
├──────────────────────────────────────────────────────────────────┤
│  停止 30 秒轮询定时器                                              │
└──────────────────────────────────────────────────────────────────┘
```

---

## 5. 原子操作

### 5.0 服务端内存锁（§8 三层保护）

> **v0.10.1 新增**：遵循 `rules.md §8` 三层 C2S 保护机制，防止同一玩家并发请求导致重复扣款/重复发货。

**三层保护**：

| 层 | 机制 | 实现 |
|----|------|------|
| ① 服务端内存锁 | `buyActive_[connKey]` / `sellActive_[connKey]` | Handler 入口设 `true`，**所有**出口（同步+异步）设 `nil` |
| ② 原子 BatchCommit | `MoneyCost + MoneyCost + ListAdd` 原子执行 | 框架保证 |
| ③ 客户端按钮禁用 | `pendingRequest_` + 倒计时确认弹窗 | UI 层防重复点击 |

**内存锁覆盖范围**：

```lua
-- BlackMerchantHandler.lua 顶部
local buyActive_  = {}  -- connKey -> true
local sellActive_ = {}  -- connKey -> true
```

- `HandleBuy`：入口检查 + 设锁，13 个出口点全部释放（含同步校验失败 4 处、异步回调失败/成功 9 处）
- `HandleSell`：入口检查 + 设锁，12 个出口点全部释放（含同步校验失败 4 处、异步回调失败/成功 8 处）
- `HandleExchange` / `HandleQuery` / `HandleHistory`：无资源授予操作，不需要内存锁

**断线清理**：`OnDisconnect(connKey)` 中同时清除 `buyActive_` 和 `sellActive_`。

### 5.1 出售操作（玩家 → 黑商）

```
C2S_BMSell { consumableId = "rake_fragment_1", amount = 1 }

HandleSell:
  1. 境界校验：IsRealmOk(connKey)
  2. 白名单校验：BMConfig.ITEMS[consumableId] 存在
  3. amount 校验：amount >= 1
  4. 读存档 backpack：CountItemHeld(backpack, consumableId) → held >= amount
     - 消耗品：CountBackpackItem(backpack, consumableId)
     - 装备类：CountEquipmentItem(backpack, cfg.equipId)
  5. 读系统库存：stock + amount <= max_stock（满仓不收）
  6. 从 backpack 扣除：
     - 消耗品：RemoveFromBackpack(backpack, consumableId, amount)
     - 装备类：RemoveEquipmentFromBackpack(backpack, cfg.equipId, amount)
       → 按 equipId 匹配，不论附魔/洗练状态
  7. WriteSaveBack（存档写回）
  8. 成功后 BatchCommit:
     MoneyAdd(userId, "xianshi", total),                    -- 加仙石
     MoneyAdd(SYSTEM_UID, "bm_stock_{id}", amount),         -- 入库
  9. 失败处理：背包已扣但 BatchCommit 失败 → 写 WAL_SELL_KEY { xianshi = total }
  10. TradeLogger.LogPublic(charName, "sell", ...)
  11. → S2C_BMResult
```

### 5.2 购买操作（黑商 → 玩家）

```
C2S_BMBuy { consumableId = "rake_fragment_1", amount = 1 }

HandleBuy:
  1. 境界校验：IsRealmOk(connKey)
  2. 白名单校验 + amount 校验
  3. 前置校验：读 playerMoneys.xianshi >= total, 读 systemMoneys.stock >= amount
  4. BatchCommit（原子操作）:
     MoneyCost(userId, "xianshi", total),                    -- 扣仙石
     MoneyCost(SYSTEM_UID, "bm_stock_{id}", amount),         -- 出库
     ListAdd(userId, WAL_BP_KEY, { consumableId, amount,     -- 背包 WAL
             itemType, equipId }),                            -- v0.10: 装备字段
  5. 成功后：读存档 → 写背包 → WriteSaveBack → 清除 WAL_BP_KEY
     - 消耗品：AddToBackpack(backpack, consumableId, amount, cfg.name)
     - 装备类：逐件 LootSystem.CreateSpecialEquipment(cfg.equipId) →
              AddEquipmentToBackpack(backpack, newEquip)（初始状态装备）
  6. TradeLogger.LogPublic(charName, "buy", ...)
  7. → S2C_BMResult
```

### 5.3 批量交易

固定价格下批量定价极其简单：

```lua
total = price × amount  -- 不再需要逐件累加
```

### 5.4 交易后立即存档

交易涉及背包变动时，通过 `WriteSaveBack` 立即将修改后的存档写回 serverCloud。

**实现要点**：
- 买入：BatchCommit 成功 → 读存档 → 写背包 → `WriteSaveBack`（异步）→ 成功后清 WAL，失败则 WAL 保留
- 卖出：先 `WriteSaveBack` 扣背包 → 成功后 `BatchCommit` 加仙石+库存 → 失败写 `WAL_SELL_KEY`
- 兑换灵韵→仙石：先 `WriteSaveBack` 扣灵韵 → 成功后 `BatchCommit` 加仙石 → 失败写 `WAL_SELL_KEY`

### 5.5 库存轮询与性能参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 轮询间隔 | **30 秒** | UI 打开期间每 30 秒发一次 C2S_BMQuery |
| C2S 全局节流 | **0.5 秒** | `THROTTLE_INTERVAL`，所有请求共享，防止短时间内重复发送 |
| 系统库存缓存 TTL | **10 秒** | `GetSystemStockCached()`，SYSTEM_UID Money 读取带缓存 |
| 缓存失效 | 买/卖成功后 | `InvalidateSystemStockCache()` 主动清缓存 |
| 轮询内容 | 库存 + 仙石余额 + 背包持有量 | 价格固定不需轮询 |
| 关闭 UI | 停止轮询 | 释放网络资源 |
| 境界缓存 | `playerRealms_` 字典 | 首次 Query 时 `EnsureRealmCached` 从存档读取，断线时清理 |

---

## 6. 商品配置表

### 6.1 掉落来源

#### 上宝逊金钯碎片（三章神器，8 种可交易）

> `rake_fragment_9`（残片·玖）不纳入黑商，因其有稳定的金币购买途径（朱老二 100 万金）。

| consumableId | 名称 | 品质 | 掉落来源 | 掉率 |
|-------------|------|------|---------|------|
| `rake_fragment_1` | 残片·壹 | red | 枯木妖王 | 0.1% |
| `rake_fragment_2` | 残片·贰 | red | 岩蟾妖王 | 0.1% |
| `rake_fragment_3` | 残片·叁 | red | 苍狼妖王 | 0.1% |
| `rake_fragment_4` | 残片·肆 | red | 赤甲妖王 | 0.1% |
| `rake_fragment_5` | 残片·伍 | red | 蛇骨妖王 | 0.1% |
| `rake_fragment_6` | 残片·陆 | red | 烈焰狮王 | 0.1% |
| `rake_fragment_7` | 残片·柒 | red | 蜃妖王 | 0.1% |
| `rake_fragment_8` | 残片·捌 | red | 黄天大圣（守关BOSS） | 0.1% |

#### 文王八卦盘碎片（四章神器，8 种）

| consumableId | 名称 | 品质 | 掉落来源 | 掉率 |
|-------------|------|------|---------|------|
| `bagua_fragment_kan` | 卦象·坎 | red | 沈渊衣 | 0.1% |
| `bagua_fragment_gen` | 卦象·艮 | red | 岩不动 | 0.1% |
| `bagua_fragment_zhen` | 卦象·震 | red | 雷惊蛰 | 0.1% |
| `bagua_fragment_xun` | 卦象·巽 | red | 风无痕 | 0.1% |
| `bagua_fragment_li` | 卦象·离 | red | 炎若晦 | 0.1% |
| `bagua_fragment_kun` | 卦象·坤 | red | 厚德生 | 0.1% |
| `bagua_fragment_dui` | 卦象·兑 | red | 泽归墟 | 0.1% |
| `bagua_fragment_qian` | 卦象·乾 | red | 司空正阳（守关BOSS） | 0.1% |

#### 天帝剑痕碎片（五章神器，当前 3 种可交易）

| consumableId | 名称 | 品质 | 掉落来源 | 掉率 |
|-------------|------|------|---------|------|
| `tiandi_fragment_1` | 剑痕·壹 | red | 仙劫战场·域外邪魔 BOSS | 0.1% |
| `tiandi_fragment_2` | 剑痕·贰 | red | 仙劫战场·域外邪魔 BOSS | 0.1% |
| `tiandi_fragment_3` | 剑痕·叁 | red | 仙劫战场·域外邪魔 BOSS | 0.1% |

#### 附灵玉（4 种，附灵师萃取产出）

| consumableId | 名称 | 品质 | 掉落来源 |
|-------------|------|------|---------|
| `lingyu_xuesha_lv1` | 血煞附灵玉 | — | 附灵师·萃取 |
| `lingyu_qingyun_lv1` | 青云附灵玉 | — | 附灵师·萃取 |
| `lingyu_fengmo_lv1` | 封魔附灵玉 | — | 附灵师·萃取 |
| `lingyu_haoqi_lv1` | 浩气附灵玉 | — | 附灵师·萃取 |

#### 炼丹材料（基础 4 种 + 中洲精华 5 种 + 龙神材料 4 种 + 金砖）

| consumableId | 名称 | 品质 | 掉落来源 | 掉率 |
|-------------|------|------|---------|------|
| `tiger_bone` | 虎骨 | orange | 虎王（初章） | 5% |
| `snake_fruit` | 灵蛇果 | orange | 蛇妖·碧鳞（贰章） | 3% |
| `diamond_wood` | 金刚木 | orange | 乌天南/乌地北（贰章） | 3% |
| `wind_eroded_grass` | 风蚀草 | orange | 沙漠九寨怪物（叁章） | 低 |
| `lipo_essence` | 力魄精华 | purple | 阵营挑战（中洲） | 阵营掉落 |
| `panshi_essence` | 磐石精华 | purple | 阵营挑战（中洲） | 阵营掉落 |
| `yuanling_essence` | 元灵精华 | purple | 阵营挑战（中洲） | 阵营掉落 |
| `shihun_essence` | 噬魂精华 | purple | 阵营挑战（中洲） | 阵营掉落 |
| `lingxi_essence` | 灵息精华 | purple | 阵营挑战（中洲） | 阵营掉落 |
| `dragon_scale_ice` | 冰龙鳞 | — | 冰龙掉落 | — |
| `dragon_scale_abyss` | 渊龙鳞 | — | 渊龙掉落 | — |
| `dragon_scale_fire` | 火龙鳞 | — | 火龙掉落 | — |
| `dragon_scale_sand` | 沙龙鳞 | — | 沙龙掉落 | — |
| `gold_brick` | 金砖 | — | — | — |

#### 特殊装备掉落（v0.10 新增，4 种戒指）

| equipId | 名称 | 品质 | 掉落来源 | 掉率 | 掉率变更 |
|---------|------|------|---------|------|---------|
| `dizun_ring_ch1` | 帝尊壹戒 | T3 orange | 虎王（初章） | 3% | v0.10: 5%→3% |
| `dizun_ring_ch2` | 帝尊贰戒 | T5 orange | 乌万仇/乌万海（贰章） | 2% | v0.10: 3%→2% |
| `dizun_ring_ch3` | 帝尊叁戒 | T7 cyan | 沙万里/狮王/蜃妖王（叁章） | — | 未变更 |
| `silong_ring_ch4` | 四龙戒指 | T9 cyan | 四龙（肆章） | — | 未变更 |

#### 五一活动信物（4 种限时）

| consumableId | 名称 | 品质 | 掉落来源 |
|-------------|------|------|---------|
| `mayday_wu` | 信物·武 | — | 五一活动 |
| `mayday_yi` | 信物·毅 | — | 五一活动 |
| `mayday_kuai` | 信物·快 | — | 五一活动 |
| `mayday_le` | 信物·乐 | — | 五一活动 |

---

## 7. 与现有系统的关系

### 7.1 前置依赖链

```
C4 Money.Cost 验证 ✅
C5 BatchCommit 验证 ✅
        │
        ↓
   万界黑商上线（无灵韵迁移依赖！无物品迁移依赖！）
        ├─ config/BlackMerchantConfig.lua（商品配置 + 汇率 + 分类）
        ├─ network/BlackMerchantHandler.lua（路由 + 交易 + 兑换 + WAL）
        ├─ network/BlackMerchantTradeLogger.lua（交易记录，公共+个人双轨）
        ├─ network/BackpackUtils.lua（背包操作共享模块）
        ├─ ui/BlackMerchantUI.lua（客户端交易面板）
        ├─ ui/BlackMerchantEntry.lua（入口按钮）
        ├─ config/zones/midland/tianji.lua（NPC "大黑无天" + 摊位装饰）
        └─ ui/NPCDialog.lua（black_merchant 交互类型）
```

**关键简化**：
- 仙石方案**完全绕过灵韵迁移**。灵韵保持存档存储不变
- **无物品 Money 域迁移**。物品持有量直接从存档背包读取，通过 `BackpackUtils` 操作

### 7.2 系统集成方式

> **与 v0.8 文档的关键差异**：实际实现**未采用自注册模式**，而是在 `server_main.lua` 中通过全局函数转发 + `RateLimitedSubscribe` 注册。

**server_main.lua 中的集成代码**（非自注册，约 20 行）：

```lua
-- 顶部 require
local BlackMerchantHandler = require("network.BlackMerchantHandler")

-- 在 session 创建后注册（RateLimitedSubscribe 提供限流保护）
RateLimitedSubscribe(SaveProtocol.C2S_BlackMerchantQuery,    "HandleBlackMerchantQuery")
RateLimitedSubscribe(SaveProtocol.C2S_BlackMerchantBuy,      "HandleBlackMerchantBuy")
RateLimitedSubscribe(SaveProtocol.C2S_BlackMerchantSell,     "HandleBlackMerchantSell")
RateLimitedSubscribe(SaveProtocol.C2S_BlackMerchantExchange, "HandleBlackMerchantExchange")
RateLimitedSubscribe(SaveProtocol.C2S_BlackMerchantHistory,  "HandleBlackMerchantHistory")

-- 全局函数转发
function HandleBlackMerchantQuery(eventType, eventData)
    BlackMerchantHandler.HandleQuery(eventType, eventData)
end
-- ... (Buy/Sell/Exchange/History 各一个转发函数)

-- 断线清理
pcall(BlackMerchantHandler.OnDisconnect, connKey)

-- 登录时 WAL 补偿检查（在 HandleLoadGame 加载存档后调用）
BlackMerchantHandler.CheckWALOnLogin(userId, slot, saveData)
```

### 7.3 系统独立性原则

| 原则 | 说明 |
|------|------|
| **Handler 独立** | 全部业务逻辑在 `BlackMerchantHandler.lua` 内部完成 |
| **配置自包含** | `BlackMerchantConfig.lua` 独立维护商品表、汇率、分类、WAL key |
| **WAL 自治** | 3 种 WAL 的写入和补偿检查均在 Handler 内部完成，通过 `CheckWALOnLogin` 在登录时触发 |
| **背包操作共享** | 通过 `BackpackUtils` 模块（`CountBackpackItem`/`AddToBackpack`/`RemoveFromBackpack`） |
| **数据隔离** | Money 域 key 以 `bm_stock_` 前缀隔离，List 域 key 以 `bm_` 前缀隔离 |
| **NPC 集成** | 天机阁地图 NPC "大黑无天" + NPCDialog 的 `interactType = "black_merchant"` |

### 7.4 与路线图的关系

| 路线图项 | 关系 |
|---------|------|
| 6.2 系统回收（NPC 回收） | **被替代**：黑商收购覆盖了 NPC 回收功能，且更有趣 |
| 6.5 拍卖行 | **互补**：拍卖行是玩家对玩家，黑商是玩家对 NPC，定位不同 |
| ~~6.1 灵韵迁移~~ | **不再依赖**：仙石方案绕过灵韵迁移 |
| ~~物品 Money 域迁移~~ | **不再需要**：物品持有量直接从背包读取 |
| B4 SaveHandler 拆分 | **无直接依赖**：黑商独立模块，不增加 B4 复杂度 |

---

## 8. UI 设计

### 8.1 设计理念

整体采用**商品橱窗**风格——玩家像逛精品店一样浏览黑商的商品陈列。每件碎片以独立的"展柜卡片"呈现，带有价签、库存指示器和操作按钮。

**NPC 肖像**：面板顶部显示 NPC "大黑无天" 头像（64×64）、名称/头衔、随机对话气泡。

### 8.2 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  [NPC头像] 大黑无天 · 万界游商      "嘿嘿，看看货色"    ✕   │  ← 标题栏（NPC对话气泡）
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─ 仙石余额 ──────────────────────────────────────────┐   │
│  │  💎 仙石: 42            [灵韵→仙石]  [仙石→灵韵]    │   │  ← 货币栏
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌材料┐ ┌活动┐ ┌附灵玉┐ ┌特殊装备┐ ┌记录┐              │  ← 第一行标签页
│  ┌逊金钯┐ ┌八卦盘┐ ┌天帝剑痕┐                          │  ← 第二行标签页（神器碎片）
│  ╔══════════════════════════════════════════════════════╗   │
│  ║                                                      ║   │
│  ║  ┌────────┐  ┌────────┐                              ║   │
│  ║  │ 图标   │  │ 图标   │  ...                         ║   │  ← 双列竖卡网格
│  ║  │ 名称   │  │ ★名称  │                              ║   │     BOSS碎片前缀 ★
│  ║  │ ▰▰░░ 3/5│  │ ▰░░░ 1/5│                              ║   │
│  ║  │ 收:10  │  │ 收:15  │                              ║   │
│  ║  │ 售:20  │  │ 售:30  │                              ║   │
│  ║  │[卖][买]│  │[卖][买]│                              ║   │
│  ║  │持有:2  │  │持有:1  │                              ║   │
│  ║  └────────┘  └────────┘                              ║   │
│  ║  ...                                                 ║   │
│  ╚══════════════════════════════════════════════════════╝   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.3 视觉风格

| 元素 | 实现值 |
|------|--------|
| 面板背景 | `rgb(22, 18, 35)` 深紫 |
| 头部区域 | `rgb(28, 22, 42)` |
| 卡片背景 | `rgb(35, 30, 52)` |
| 仙石余额文字 | `rgb(180, 140, 255)` 仙石紫 |
| 库存条颜色 | 绿（>3）→ 黄（2~3）→ 红（≤1） |
| BOSS碎片 | 名称前缀 ★，视觉加强 |
| 道具图标 | 支持 PNG 图片（`image` 字段）和 emoji 文字（`icon` 字段） |

### 8.4 交互细节

| 交互 | 行为 |
|------|------|
| 点击 [出售] | 弹出确认弹窗（3秒倒计时自动关闭），确认后发 C2S_BMSell |
| 点击 [购买] | 弹出确认弹窗（3秒倒计时），确认后发 C2S_BMBuy |
| 点击 [灵韵→仙石] | 弹出兑换弹窗：+1/-1/+5/-5/MAX 数量调整，实时预览消耗/获得 |
| 点击 [仙石→灵韵] | 弹出兑换弹窗：同上，提示 50% 折损 |
| 切换"记录"标签 | 发 C2S_BMHistory 拉取公共+个人合并记录（≤100 条） |
| 按钮不可用 | 仙石不足/库存满/无货/持有量为0 → 对应按钮置灰 |
| 交易成功 | 状态提示更新 + 仙石显示刷新 |
| 交易失败 | 状态提示显示错误原因 |
| 防重复请求 | `pendingRequest_` 标志 + `THROTTLE_INTERVAL = 0.5s` 全局节流 |
| 状态提示保护 | `statusKeepUntil_` 防止高优先级消息被低优先级覆盖 |

---

## 9. 代码模块结构

### 9.1 目录结构

```
scripts/
├── server_main.lua                          -- 注册5个Handler + 登录WAL补偿 + 断线清理
├── main.lua                                 -- 客户端入口（创建入口按钮 + Update轮询转发）
│
├── config/
│   └── BlackMerchantConfig.lua              -- 商品配置表 + 常量 + 分类 + WAL key（~210行）
│                                            -- v0.10: 新增 EquipItemFromData() + CATEGORY_SPECIAL_EQUIP
│
├── network/
│   ├── BlackMerchantHandler.lua             -- 服务端 Handler: Query/Buy/Sell/Exchange/History + WAL（~640行）
│   │                                        -- v0.10: 引入 LootSystem, 装备买卖分支 + CountItemHeld
│   │                                        -- v0.10.1: +buyActive_/sellActive_ 内存锁（§8 三层保护）
│   ├── BlackMerchantTradeLogger.lua         -- 交易记录写入: 公共/个人双轨 + FIFO淘汰（~80行）
│   ├── BackpackUtils.lua                    -- 背包操作共享模块（Count/Add/Remove）
│   │                                        -- v0.10: +CountEquipmentItem/AddEquipmentToBackpack/RemoveEquipmentFromBackpack
│   └── SaveProtocol.lua                     -- 网络协议定义（9个事件ID）
│
├── ui/
│   ├── BlackMerchantUI.lua                  -- 客户端交易面板（~1600行）
│   │                                        -- v0.10: 双行页签 + 特殊装备标签
│   ├── BlackMerchantEntry.lua               -- 入口按钮（小地图下方，~70行）
│   └── NPCDialog.lua                        -- NPC对话（interactType="black_merchant" 触发打开面板）
│
├── config/zones/midland/
│   └── tianji.lua                            -- 天机阁地图（NPC "大黑无天" 定义 + 摊位装饰）
│
├── rendering/
│   └── DecoDivine.lua                        -- RenderMerchantStall（黑商摊位NanoVG装饰渲染）
│
└── tests/
    └── test_black_merchant.lua               -- 集成测试（MockCloud/MockSession, 4组用例）
```

### 9.2 BlackMerchantConfig 关键常量

```lua
BMConfig.MAX_STOCK           = 5      -- 默认库存上限
BMConfig.XIANSHI_BUY_RATE    = 100    -- 灵韵→仙石汇率
BMConfig.XIANSHI_SELL_RATE   = 50     -- 仙石→灵韵汇率
BMConfig.SYSTEM_UID          = 0      -- 系统用户ID
BMConfig.JINDAN_ORDER        = 4      -- 解锁门槛：筑基初期

BMConfig.TRADE_LOG_KEY       = "bm_trade_log"    -- 个人兑换记录key
BMConfig.PUBLIC_TRADE_LOG_KEY= "bm_pub_trade"    -- 公共买卖记录key
BMConfig.MAX_TRADE_RECORDS   = 50                -- 公共记录上限
BMConfig.MAX_PERSONAL_RECORDS= 50                -- 个人记录上限

BMConfig.WAL_KEY             = "bm_wal"          -- WAL：灵韵补偿
BMConfig.WAL_BP_KEY          = "bm_wal_bp"       -- WAL：背包物品补偿
BMConfig.WAL_SELL_KEY        = "bm_wal_sell"     -- WAL：仙石补偿

-- 分类标签页（v0.10: 8 分类，双行布局）
BMConfig.CATEGORY_NAMES = {
    material      = "材料",       rake   = "逊金钯",    bagua  = "八卦盘",
    tiandi        = "天帝剑痕",   lingyu = "附灵玉",    event  = "活动",
    special_equip = "特殊装备",   -- v0.10 新增
}
-- 页签布局：第一行 材料|活动|附灵玉|特殊装备|记录
--          第二行 逊金钯|八卦盘|天帝剑痕
```

### 9.3 WAL 补偿机制（3 种 WAL）

`CheckWALOnLogin(userId, slot, saveData, onDone)` 在玩家登录时自动执行，**并行检查三种 WAL**：

| WAL key | 触发场景 | 补偿方式 |
|---------|---------|---------|
| `bm_wal` | 仙石→灵韵兑换：Cost 成功但灵韵未到账 | 灵韵写入存档 `saveData.player.lingYun` → `WriteSaveBack` |
| `bm_wal_bp` | 购买：仙石已扣+库存已扣但背包未写入 | 消耗品：`AddToBackpack` → `WriteSaveBack`；装备类（v0.10）：逐件 `CreateSpecialEquipment` → `AddEquipmentToBackpack` → `WriteSaveBack` |
| `bm_wal_sell` | 出售：背包已扣但仙石未到账；或灵韵→仙石：灵韵已扣但仙石 Add 失败 | `BatchCommit:MoneyAdd(xianshi)` 直接补发 |

**v0.10 WAL 数据扩展**：`bm_wal_bp` 条目新增 `itemType` 和 `equipId` 字段，`CheckWALOnLogin` 据此判断补偿装备还是消耗品。

**补偿完成后清除对应 WAL 条目**（`serverCloud.list:Delete`）。

---

## 10. 风险与缓解

| 风险 | 严重度 | 缓解方案 |
|------|--------|---------|
| **兑换崩溃导致资产丢失** | 🟡 中 | 3种WAL覆盖所有崩溃场景（§9.3），登录时自动补偿 |
| **买入后背包未写入** | 🟡 中 | `WAL_BP_KEY` 记录物品信息，登录时 `AddToBackpack` 补偿 |
| **出售后仙石未到账** | 🟡 中 | `WAL_SELL_KEY` 记录仙石数，登录时 `BatchCommit` 补发 |
| **BatchCommit 失败** | 🟢 低 | 交易失败，客户端提示"请重试"。无资产损失 |
| **恶意刷库存** | 🟢 低 | 库存上限约束总量。小号也需要先获得物品（BOSS 掉落），成本不低 |
| **碎片稀缺度破坏** | 🟡 中 | 库存上限 5，售价 2 倍收购价。碎片仍然稀缺，只是多了一个应急渠道 |
| **仙石通胀** | 🟢 低 | 仙石只能兑换获得（100 灵韵 = 1 仙石），本质是灵韵的代币化 |
| **SYSTEM_UID 热读** | 🟢 低 | 10 秒 TTL 缓存 + 30 秒客户端轮询，有效降低读频率 |
| ~~**灵韵迁移风险**~~ | ~~🟠~~ | ✅ **已消除**：仙石方案绕过灵韵迁移 |
| ~~**物品迁移风险**~~ | ~~🟠~~ | ✅ **已消除**：背包直接操作，无 Money 域迁移 |
| ~~**价格竞态**~~ | ~~🟡~~ | ✅ **已消除**：固定价格无竞态 |

---

## 11. NPC 与地图集成

### 11.1 NPC "大黑无天"

- **位置**：中洲·天机阁地图区域（`config/zones/midland/tianji.lua`）
- **交互类型**：`interactType = "black_merchant"`
- **对话触发**：通过 `NPCDialog.lua` 映射到 `BlackMerchantUI.Show()`
- **摊位装饰**：配套 `merchant_stall` 装饰物，通过 `DecoDivine.RenderMerchantStall` 渲染（NanoVG）

### 11.2 双入口

| 入口 | 位置 | 实现 |
|------|------|------|
| 功能图标 | 小地图容器下方（始终可见） | `BlackMerchantEntry.lua` |
| NPC 对话 | 天机阁·大黑无天 NPC | `NPCDialog.lua` → `interactType = "black_merchant"` |

---

## 12. 待讨论事项

| # | 问题 | 说明 |
|---|------|------|
| 1 | 仙石余额上限 | 是否需要设置仙石持有上限（防止囤积），或不设限 |
| 2 | 活动商品下架机制 | 五一活动结束后，信物类商品是否自动下架，还是保留但不再新增来源 |
| 3 | 金砖定价微调 | 金砖 `sell_price = 5`（`buy_price × 2.5`），是否统一为 `buy_price × 2 = 4` |

---

## 变更日志

| 版本 | 日期 | 变更 |
|------|------|------|
| v0.1 | 2026-04-09 | 初版：基于灵韵的 NPC 交易系统 |
| v0.2 | 2026-04-09 | 细化定价公式，添加 UI 入口描述 |
| v0.3 | 2026-04-09 | 添加二阶段锁协议，完善原子操作 |
| v0.4 | 2026-04-09 | 修正库存上限 S=6，移除所有交易限额 |
| v0.5 | 2026-04-09 | 添加交易后立即存档，补充 S2C 玩家持有量字段 |
| v0.6 | 2026-04-10 | **重大变更**：引入仙石货币，绕过灵韵迁移；固定价格取代动态定价；碎片8溢价；单步交易取代二阶段锁；入口逻辑改为开始即可见、筑基初期解锁操作；新增商品橱窗 UI 设计 |
| v0.7 | 2026-04-10 | 移除 `rake_fragment_9`；新增交易记录系统（List 域）；系统独立性原则；独立目录结构 |
| v0.7.1 | 2026-04-10 | 实施前一致性审查 + API 对齐 |
| v0.8 | 2026-04-24 | 解锁前置下调至筑基初期；新增炼丹材料页签（9 种）；商品 19→28 |
| v0.9 | 2026-05-04 | **代码对齐全面修订**：商品 28→41 种（+龙神材料4、+金砖1、+附灵玉4、+五一活动4）；分类从 4 扩展到 7 个标签页（材料/活动/逊金钯/八卦盘/天帝剑痕/附灵玉/记录）；**废除物品 Money 域迁移方案**，改为背包直操作（`BackpackUtils`）；WAL 从 1 种扩展为 3 种（灵韵/背包物品/仙石）；交易记录改为公共+个人双轨（各50条）；轮询 10s→30s + 10s TTL 系统缓存 + 0.5s C2S 节流；协议事件 ID 对齐代码（`C2S_BMQuery` 等）；目录结构对齐实际代码（`config/` + `network/` + `ui/`，非 `BlackMerchant/` 独立目录）；集成方式修正为 server_main 全局函数转发（非自注册）；新增 NPC "大黑无天" + 天机阁地图集成；新增金砖定价异常标注 |
| v0.10 | 2026-05-04 | **特殊装备品类 + 双行页签**：商品 41→45 种（+帝尊戒指3枚、+四龙戒指1枚）；分类 7→8 个（新增 `special_equip` 特殊装备）；**页签改为双行布局**（第一行：材料/活动/附灵玉/特殊装备/记录，第二行：逊金钯/八卦盘/天帝剑痕）；**装备类买卖支持**：卖出接受任意附魔/洗练状态，买入创建初始状态装备（`LootSystem.CreateSpecialEquipment`）；`BackpackUtils` 新增 3 个装备操作函数（`CountEquipmentItem`/`AddEquipmentToBackpack`/`RemoveEquipmentFromBackpack`）；`BlackMerchantHandler` 引入 `LootSystem` 依赖，新增 `IsEquipmentItem`/`CountItemHeld` 分派函数；WAL `bm_wal_bp` 数据结构扩展 `itemType`+`equipId` 字段；帝尊壹戒掉率 5%→3%（`MonsterTypes_ch1.lua`），帝尊贰戒掉率 3%→2%（`MonsterTypes_ch2.lua`）；`BlackMerchantConfig` 新增 `EquipItemFromData()` 辅助函数从 `EquipmentData` 读取装备模板 |
| v0.10.1 | 2026-05-04 | **服务端内存锁 + 错误文本修正**：`BlackMerchantHandler` 新增 `buyActive_`/`sellActive_` 内存锁（遵循 rules.md §8 三层 C2S 保护），HandleBuy 13 个出口点、HandleSell 12 个出口点全部释放锁；`OnDisconnect` 清理两个锁字典；新增 §5.0 文档章节；出售校验错误文本 `"未持有此碎片"` → `"未持有此物品"`（适配装备类商品） |

---

*最后更新：2026-05-04*
*版本：v0.10.1（服务端内存锁 + 错误文本修正）*
