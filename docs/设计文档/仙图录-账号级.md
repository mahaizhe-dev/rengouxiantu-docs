# 仙图录系统设计方案

> **状态**: 草案 v0.8 — 属性已定义  
> **负责**: AI 辅助设计  
> **当前实现状态**: 代码已休眠，等待方案确认后激活

---

## 1. 系统定位

仙图录是**账号级收集与养成系统**，记录玩家的里程碑和收藏品，提供账号级属性加成。

- 道印：修行里程碑铭刻，提供属性加成，部分可升级
- 神器：收集养成，展示贴图、激活情况和属性加成

**核心原则**:
- 所有数据为**账号级**，任意角色的行为都可以推进进度
- 属性加成作用于**账号内所有角色**

---

## 2. 系统架构

### 2.1 存储方案：独立 CloudStorage key（账号级）

```
account_atlas = { ... }          -- 仙图录独立 key（勋章 + 神器）
save_1 = { player = {...} }     -- 角色1存档
save_2 = { player = {...} }     -- 角色2存档
```

- 数据不绑定角色槽，切换角色不影响
- 任意角色的行为（境界突破、游戏时长等）都推进账号级进度

### 2.2 即时存储规则

道印升级、神器碎片激活等**不可逆进度变更**，必须在操作完成后**立即写入 CloudStorage**，不依赖定时存档。

| 触发时机 | 存储内容 |
|---------|---------|
| 道印升级（含首次获得） | `account_atlas.medals` 整段 |
| 神器碎片激活 | `account_atlas.artifact` 整段 |
| 神器 BOSS 击败 | `account_atlas.artifact` 整段 |

> 避免玩家达成条件后因意外退出导致进度丢失。

---

## 3. 入口与展示规则

### 3.1 入口位置

仙图录入口在**登录界面**，替代原来的"退出游戏"按钮。

| 字段 | 说明 |
|------|------|
| 位置 | `LoginScreen` 按钮列表，原"退出游戏"按钮位置 |
| 按钮文本 | "仙图录" |
| 点击行为 | 打开仙图录全屏面板（覆盖登录界面） |

> 退出游戏功能移至仙图录面板内或角色选择界面，登录界面不再直接提供退出按钮。

### 3.2 道印隐藏规则

**无法获得且未获得**的道印在列表中**隐藏**，不展示给玩家。

| 条件 | 是否显示 | 示例 |
|------|---------|------|
| 已获得（任意等级） | 显示 | 境界大师 Lv3 |
| 未获得但可获得 | 显示（灰色/锁定态） | 青云试炼（未通关 30 层） |
| 未获得且不可获得 | **隐藏** | 先人一步·化神（名额已满 10 人） |

**判断"不可获得"的逻辑**：

| trigger.type | 不可获得条件 |
|-------------|-------------|
| `server_race` | 排行榜已满（已达到 `max_slots` 上限） |
| `value_check` | 永远可获得（不会隐藏） |
| `item_consume` | 永远可获得（不会隐藏） |

> 已获得的道印**永远显示**，即使是后来名额已满的 `server_race` 类道印。

---

## 4. 仙图录两大板块

| 板块 | 章节 | 说明 |
|------|------|------|
| **道印** | §5 | 修行铭刻，提供属性加成，共 9 枚 |
| **神器** | §6 | 神器贴图、激活、属性加成（账号级共享） |

---

## 5. 道印系统

### 5.1 定义

道印 = **修行里程碑的铭刻**，记录修仙路上的关键成就，提供账号级属性加成。

- 每枚道印都有**属性加成**（非纯展示）
- 部分道印可**升级**，属性随等级递增
- 全部为**账号级**，任意角色的行为都可推进进度

### 5.2 图标资源

每枚道印需要一张**大尺寸专属图标**，用于弹窗展示和仙图录列表。

| 字段 | 规格 |
|------|------|
| 尺寸 | 256 × 256 px |
| 格式 | PNG，透明背景 |
| 风格 | 仙侠篆刻风，金色/青铜主色调，带光晕底纹 |
| 路径 | `Textures/atlas/seal_{id}.png`（如 `seal_realm_master.png`） |

### 5.3 获得表现

道印首次获得或升级时，触发**全屏弹窗**：

| 阶段 | 表现 |
|------|------|
| 铭刻动画 | 道印大图标（256px）从虚到实浮现，金光涟漪向四周扩散 |
| 信息展示 | 道印名称 + 当前等级 + 本次新增属性加成 |
| 退出 | 点击任意位置关闭，或 2 秒后自动消失 |

> 弹窗期间**不暂停游戏**，仅叠加 UI 层。

### 5.4 统一结构

所有道印遵循同一定义模板：

| 字段 | 说明 |
|------|------|
| `id` | 唯一标识 |
| `name` | 显示名称 |
| `type` | `levelable`（可升级）或 `one_time`（一次性） |
| `max_level` | 最高等级（一次性 = 1） |
| `trigger.type` | 触发类型（见下） |
| `trigger.source` | 检测数据来源 |
| `trigger.thresholds` | 每级解锁阈值 |
| `bonus.attrs` | 加成属性 |
| `bonus.per_level` | 每级加成值 |

**三种触发类型**：

| trigger.type | 逻辑 | 适用道印 |
|-------------|------|---------|
| `value_check` | 取账号内某数值的最大值，达到阈值即升级 | 境界大师、职业四印、青云试炼 |
| `item_consume` | 消耗指定道具累积经验，每满额升 1 级 | 仙途守护者 |
| `server_race` | 全服前 N 名达成条件，一次性获得 | 先人一步 ×2 |

### 5.5 全部道印

#### ① 数值检测类（value_check）— 6 枚

| ID | 名称 | 级数 | 检测来源 | 阈值 | 每级加成 | 满级总加成 |
|----|------|------|---------|------|---------|----------|
| `medal_realm_master` | 境界大师 | 8 | 任意角色最高大境界 | 练气~渡劫（每境 +1 级） | +30 HP | +240 HP |
| `medal_taixu` | 太虚之道 | 10 | 最高太虚（剑修）角色等级 | 10, 20, …, 100 | +1 悟性 | +10 悟性 |
| `medal_zhenyue` | 镇岳之力 | 10 | 最高镇岳（体修）角色等级 | 10, 20, …, 100 | +1 体魄 | +10 体魄 |
| `medal_luohan` | 罗汉之躯 | 10 | 最高罗汉（武僧）角色等级 | 10, 20, …, 100 | +1 根骨 | +10 根骨 |
| `medal_tianyan` | 天衍之机 | 10 | 最高天衍（阵法师）角色等级 | 10, 20, …, 100 | +1 福源 | +10 福源 |
| `medal_trial` | 青云试炼 | 8 | 试炼历史最高通关层数 | 30, 60, …, 240（每 30 层） | +4 DEF | +32 DEF |

> 青云试炼阈值当前为每 30 层一级（对应境界段），后续高境界段可能调整为 40 层。

#### ② 道具消耗类（item_consume）— 1 枚

| ID | 名称 | 级数 | 道具 | 每级经验 | 每级加成 |
|----|------|------|------|---------|---------|
| `medal_guardian` | 仙途守护者 | 无上限 | 仙途印符（`item_guardian_token`） | 100 | +1 根骨/悟性/福缘/体魄 |

> 仙途印符为运营发放的消耗品，每使用 1 枚 +100 经验。用于奖励积极反馈 BUG、提供有效建议的玩家。

#### ③ 全服竞速类（server_race）— 2 枚

| ID | 名称 | 名额 | 达成条件 | 加成 |
|----|------|------|---------|------|
| `medal_pioneer_huashen` | 先人一步·化神 | 10 | 全服前 10 名突破化神 | +5 ATK |
| `medal_pioneer_heti` | 先人一步·合体 | 10 | 全服前 10 名突破合体 | +5 ATK |

> 服务端验证：玩家突破时提交排行榜，未满名额则解锁，已满则显示"已绝版"。

### 5.6 数据结构

```lua
-- account_atlas.medals
{
    -- 数值检测类
    realm_master_level = 0,    -- 境界大师（0~8）
    taixu_level        = 0,    -- 太虚之道（0~10）
    zhenyue_level      = 0,    -- 镇岳之力（0~10）
    luohan_level       = 0,    -- 罗汉之躯（0~10）
    tianyan_level      = 0,    -- 天衍之机（0~10）
    trial_level        = 0,    -- 青云试炼（0~8）

    -- 道具消耗类
    guardian_exp        = 0,    -- 仙途守护者累计经验

    -- 全服竞速类
    pioneer_huashen    = false, -- 先人一步·化神
    pioneer_heti       = false, -- 先人一步·合体
}
```

### 5.7 属性生效

道印属性为**账号级全局加成**，在角色属性计算时叠加：

```
HP  += 境界大师等级 × 30
根骨 += 仙途守护者等级 × 1 + 罗汉之躯等级 × 1
悟性 += 仙途守护者等级 × 1 + 太虚之道等级 × 1
福缘 += 仙途守护者等级 × 1 + 天衍之机等级 × 1
体魄 += 仙途守护者等级 × 1 + 镇岳之力等级 × 1
DEF += 青云试炼等级 × 4
ATK += (化神已解锁 ? 5 : 0) + (合体已解锁 ? 5 : 0)
```

---

## 6. 神器系统

### 6.1 概述

神器是**账号级收集养成**内容，记录表现贴图、激活情况和属性加成。

**关键规则**：神器进度为账号级共享。角色A激活的碎片，角色B也会激活，无需重复操作。

### 6.2 上宝逊金钯

| 字段 | 值 |
|------|------|
| ID | `artifact_rake` |
| 名称 | 上宝逊金钯 |
| 背景 | 天蓬元帅旧兵器 |
| 贴图 | `Textures/artifact/rake_full.png` |
| 结构 | 3×3 九宫格（9个碎片格） |

#### 碎片与激活

每格需要消耗对应碎片 + 资源来激活：

| 资源 | 单格消耗 |
|------|---------|
| 金币 | 500,000 |
| 灵韵 | 500 |
| 对应碎片 | 1 个（`rake_fragment_1` ~ `rake_fragment_9`） |

> 第9片碎片从朱老二处购买，费用 100万金币。

#### 属性加成

| 条件 | 重击值 | 根骨 | 福源 |
|------|--------|------|------|
| 每激活 1 格 | +30 | +10 | +10 |
| 9 格全满额外 | +30 | +10 | +10 |
| **全激活总计** | **300** | **100** | **100** |

#### 被动技能：天蓬遗威

| 字段 | 值 |
|------|------|
| 解锁条件 | 9格全激活 + 击败BOSS「猪二哥」 |
| 触发概率 | 15% |
| 伤害 | 攻击力 × 50%（真实伤害，无视防御） |
| 内置CD | 2.0 秒 |

#### BOSS战：猪二哥

| 字段 | 值 |
|------|------|
| BOSS ID | `artifact_zhu_erge` |
| 挑战条件 | 9格全部激活 |
| 竞技场 | 独立 8×8 竞技场（备份/恢复世界状态） |
| 失败惩罚 | 无（满血复活） |
| 胜利奖励 | 解锁被动技能「天蓬遗威」 |

### 6.3 天帝剑痕

| 字段 | 值 |
|------|------|
| ID | `artifact_tiandi` |
| 名称 | 天帝剑痕 |
| 背景 | 上古天帝斩魔时遗留的剑意，封印于中洲封魔谷混沌石台之上 |
| 贴图 | `image/divine_tiandi_20260417170838.png` |
| 结构 | 3×3 九宫格（9个碎片格，当前仅实现前3片） |
| 世界位置 | 中洲·封魔谷（x=40, y=33），替代原封魔大阵 |

#### 碎片与激活

| 字段 | 值 |
|------|------|
| 碎片来源（1-3片） | 仙劫战场·域外邪魔 BOSS |
| 碎片来源（4-9片） | 星魂战场 / 天裂战场（待实现） |
| 当前已实现 | 碎片·壹~叁（3片），碎片·肆~玖待后续扩展 |

| 资源 | 单格消耗 |
|------|---------|
| 金币 | 1,000,万 |
| 灵韵 | 2,000 |
| 对应碎片 | 1 个（`tiandi_fragment_1` ~ `tiandi_fragment_9`） |

#### 属性加成

| 条件 | 攻击力 | 击杀回血 | 每秒回血 |
|------|--------|---------|---------|
| 每激活 1 格 | +10 | +4.5 HP | +1 HP/s |
| **3格全满（当前上限）** | **+30** | **+13.5 HP** | **+3 HP/s** |
| 9格全满（满级理论值） | +90 | +40.5 HP | +9 HP/s |

> 击杀回血 +4.5 HP/格 对标 T2 回血档，属于中端回血加成。

#### 被动技能与 BOSS（待实现）

| 内容 | 状态 |
|------|------|
| 被动技能 | 天帝剑斩·暴击减CD（效果：暴击时降低技能冷却时间，具体数值待定） |
| BOSS战 | 待实现（名称/机制待定） |

> 当前版本（碎片4-9及BOSS/被动）均显示为"🔲 暂未实装"占位，不影响已激活格子的属性加成正常生效。

#### 碎片ID对照

| 碎片 | ID | 状态 |
|------|-----|------|
| 碎片·壹 | `tiandi_fragment_1` | ✅ 已实现（仙劫战场掉落） |
| 碎片·贰 | `tiandi_fragment_2` | ✅ 已实现（仙劫战场掉落） |
| 碎片·叁 | `tiandi_fragment_3` | ✅ 已实现（仙劫战场掉落） |
| 碎片·肆~玖 | `tiandi_fragment_4~9` | 🔲 待实现（星魂/天裂战场） |

### 6.4 存储结构

```lua
-- account_atlas 中的神器数据
{
    artifact = {
        activatedGrids = { false, false, ..., false },  -- 9个布尔值
        bossDefeated = false,
        passiveUnlocked = false,
    },
    artifact_tiandi = {
        activatedGrids = { false, false, ..., false },  -- 9个布尔值，当前有效索引1~3
        bossDefeated = false,      -- 待实现
        passiveUnlocked = false,   -- 待实现
    },
}
```

**迁移方案**（已确认）：当前神器存储在角色存档中，迁移至账号级时，取所有角色中激活数最多的一份作为基准。

### 6.5 现有代码

| 文件 | 说明 |
|------|------|
| `systems/ArtifactSystem.lua` | 上宝逊金钯核心逻辑（激活、BOSS战、被动技能） |
| `ui/ArtifactUI.lua` | 上宝逊金钯拼图式九宫格 UI |
| `systems/ArtifactSystem_tiandi.lua` | 天帝剑痕核心逻辑（激活、属性加成） |
| `ui/ArtifactUI_tiandi.lua` | 天帝剑痕九宫格 UI（前3格可激活，4-9格锁定占位） |
| `rendering/DecoDivine.lua` | 世界装饰渲染（包含 `RenderDivineTiandi` 金红剑痕特效） |
| `config/zones/midland/fengmo.lua` | 中洲·封魔谷放置位置（NPC + 装饰，替代原封魔大阵） |

---

## 修订记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-04-01 | 初稿：基于已有代码整理设计方案 |
| v0.2 | 2026-04-02 | 确定账号级独立存储、3模块结构 |
| v0.3 | 2026-04-02 | 用户定义成就列表（89个） |
| v0.4 | 2026-04-02 | 新增徽章系统（3个） |
| v0.5 | 2026-04-02 | 新增神器、称号；移除早期设定 |
| v0.6 | 2026-04-02 | 大幅精简：仅保留勋章+神器两大板块；移除成就和称号 |
| v0.7 | 2026-04-02 | 属性标为"待定"；游戏时长改为游戏内统计；神器迁移方案确认 |
| v0.8 | 2026-04-02 | 移除游戏时间勋章（无法统计历史时长）；确定全部属性：境界大师+30HP/级、仙途守护者+1根骨/悟性/福缘/体魄每级、先人一步均+5攻击力 |
