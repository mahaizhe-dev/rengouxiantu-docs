# 第六章 · NPC功能文档

> 状态：已落地主线 NPC、基础交互对象、仓库入口、中央两界村封印、青莲长生体与两界阵石完整链路
> 最后更新：2026-07-14
> 用途：记录第六章 NPC、交互对象、主线任务、封印边界与后续迭代结论。
> 关联代码：`scripts/config/ZoneData_ch6.lua`、`scripts/config/QuestData_ch6.lua`、`scripts/systems/QuestSystem.lua`
> 关联设计：[第六章-令牌养成系统文档.md](第六章-令牌养成系统文档.md)

---

## 一、当前基线

| 项目 | 当前口径 |
|------|---------|
| 章节 | 第六章 · 两界村之影 |
| 地图 | 复用第一章区域轮廓，但运行期实体、怪物、装饰、封印与 NPC 独立 |
| 视觉语言 | 第六章统一使用水晶 / 晶痕；东南区为界守东大营，不再按蜘蛛洞表现 |
| 出生点 | `spawnPoint = (76.5, 40.5)`，位于 `shadow_spawn_safe` |
| 安全区 | `shadow_spawn_safe`，范围约 `x=72~79, y=35~45` |
| 仓库 | `warehouse_chest_ch6`，位于安全区 `(78, 37)`，复用前五章百宝箱素材、仓库 UI 与角色仓库存档 |
| 主线 NPC | `ch6_quest_npc`，名称“村长”，位于 `(75, 42)` |
| 主线链 ID | `shadow_village_zone` |
| 中央封印 | `ch6_shadow_village`，主线第 8 步解除 |
| 虎王试炼封印 | `seal_ch6_tiger_trial`，未来副本封印，`futureLocked = true`，当前无任何解封入口 |
| 两界阵石 | 两界村外四角各 1 根阵法石柱，使用谪仙令激活并培养至 30 级，分别提供攻击 / 防御 / 生命 / 生命回复 |

---

## 二、设计原则

1. **不污染第一章**：第六章不复用第一章 NPC、装饰、刷怪点和任务链；功能相似时也必须使用第六章独立 ID。
2. **ID 前缀统一**：NPC 与交互对象使用 `ch6_` 前缀；主线链使用独立 `shadow_village_zone`，避免复用 `town_zone`。
3. **封印边界清晰**：第 8 步只解开中央两界村封印；虎王试炼封印是未来副本封印，不随主线、GM 常规解封、读档同步或任务完成自动开启。
4. **系统入口沿用成熟框架**：主线 NPC 沿用 `VillageChiefUI` + `QuestSystem`，封印沿用 `QuestData.SEALS`、`QuestSystem.ApplySeals/Unseal` 和地图独立阻挡层。
5. **天青白莲独立养成**：天青白莲用于青莲长生体获取，需使用第六章独立 ID、独立进度与独立消耗；交互流程可参考福源果，但不得复用福源果存档字段。
6. **两界阵石独立于主线封印**：阵石是第六章谪仙令的长期养成入口，不负责解开中央村门、虎王试炼或其他副本封印，也不复用海神柱 / 祀剑池存档字段。
7. **仓库入口复用成熟系统**：第六章只新增独立世界对象 ID，不新增第六章仓库数据；`warehouse` 交互继续打开统一 `WarehouseUI`，物品、解锁行数与保存流程继续使用现有角色仓库存档。

---

## 三、核心功能 NPC

| ID | 名称 | 类型 | 区域 | 坐标 | 交互 | 状态 |
|----|------|------|------|------|------|------|
| `ch6_teleport_array` | 传送法阵 | 章节基础设施 | `shadow_spawn_safe` | `(76, 39)` | `teleport_array` | 已落地，带通用传送阵特效 |
| `ch6_quest_npc` | 村长 | 区域主线 | `shadow_spawn_safe` | `(75, 42)` | `village_chief` | 已落地，绑定 `shadow_village_zone` |
| `ch6_alchemy` | 炼丹炉 | 仙阶材料入口 | `shadow_spawn_safe` | `(78, 41)` | `alchemy` | 已落地 |
| `ch6_merchant` | 装备商人 | 命格商店 | `shadow_spawn_safe` | `(73, 39)` | `sell_equip` | 已落地，第六章独立入口，只售 T1 紫色命格 |
| `warehouse_chest_ch6` | 百宝箱 | 仓库存取入口 | `shadow_spawn_safe` | `(78, 37)` | `warehouse` | 已落地，复用前五章素材、统一仓库 UI 与角色仓库存档 |
| `ch6_qingbai_lotus_01` ~ `ch6_qingbai_lotus_09` | 天青白莲 | 仙体获取对象 | 呱大人区域莲湖周边 | 4 颗在湖心 BOSS 岛，5 颗沿西岸 / 东岸池水外圈散布 | `qinglian_body_lotus` | 已实装，独立存档进度与一次性激活流程 |
| `ch6_liangjie_stone_xuanbi` | 两界阵石·玄壁 | 防御养成对象 | 两界村西北角外侧 | `(32, 32)` | `liangjie_stone` | 已实装 |
| `ch6_liangjie_stone_tianfeng` | 两界阵石·天锋 | 攻击养成对象 | 两界村东北角外侧 | `(49, 32)` | `liangjie_stone` | 已实装 |
| `ch6_liangjie_stone_houtu` | 两界阵石·厚土 | 生命养成对象 | 两界村西南角外侧 | `(32, 49)` | `liangjie_stone` | 已实装 |
| `ch6_liangjie_stone_huiyuan` | 两界阵石·回元 | 回复养成对象 | 两界村东南角外侧 | `(49, 49)` | `liangjie_stone` | 已实装 |

### 3.1 主线 NPC 背景

第六章主线 NPC 为村长。原来两界村的村长并非凡人，而是镇守仙凡交界之地的仙人。两界村原本是仙凡交界之地，有天兵镇守，秩序稳定且极为安全；但不知仙界出了什么状况，魔君来犯时迟迟没有任何援军。留守两界村的仙兵、仙将在绝望中放弃了自己的使命，拒绝反攻两界村，只维持残存防线与自保秩序。

---

### 3.2 青莲长生体获取

实现状态：已实装。代码落点为 `scripts/systems/QinglianBodySystem.lua`、`scripts/config/ZoneData_ch6.lua`、`scripts/config/MonsterTypes_ch6.lua`、`scripts/config/GameConfig.lua`、`scripts/config/BlackMerchantConfig.lua`、`scripts/config/AscensionConfig.lua`、`scripts/ui/ImmortalBodyPanel.lua` 与存档 DTO / 保存 / 读档链路。

| 项目 | 规则 |
|------|------|
| 交互对象 | 天青白莲 |
| 数量 | 9 颗，从原计划 4 颗调整为 9 颗 |
| 分布 | 呱大人区域莲湖周边 9 颗；4 颗在湖心 BOSS 岛，5 颗沿西岸 / 东岸池水外圈散布，形成水边与岸边交错站位 |
| 状态 | 每颗白莲有未激活 / 已激活两种状态 |
| 激活表现 | 已激活状态下，NPC 名字呈现渐变青白色，并带类似福源果的光效 |
| 激活消耗 | 莲池仙露 ×1 + 2万灵韵 |
| 莲池仙露来源 | 蛤蟆仙人 0.2% 掉落；呱大人 0.05% 掉落 |
| 道具品级 | 莲池仙露为圣器级消耗品 |
| 黑市口径 | 莲池仙露进入黑市-消耗品；定价 60 / 30 |
| 单颗奖励 | 激活成功后一次性获得 1000万经验 |
| 激活提示 | 激活成功时弹出成功提示 |
| 激活流程 | 交互过程与界面参考福源果；每次激活界面显示当前激活进度，例如 3/9 |
| 全部激活奖励 | 9 颗全部激活后，弹出获取展示框，表现形式参考节日活动抽奖获得展示 |
| 最终获得 | 新仙体：青莲长生体 |
| 仙体属性 | 成长模板：生命 +30/级，生命恢复 +1/级，攻击 +3/级，防御 +2/级 |
| 解锁口径 | 只解锁青莲长生体，不自动切换当前角色仙体 |
| 仙体资源 | 需要新增仙体整卡资源；参考 `body_immortal_1` 的物象剪影、卡背、艺术字规格 |

实现边界：

- 已采用系统 ID：仙体 `immortal_body_qinglian`，交互类型 `qinglian_body_lotus`，消耗品 `lotus_pool_dew`。
- 9 颗天青白莲已使用稳定编号 `ch6_qingbai_lotus_01` 至 `ch6_qingbai_lotus_09`，当前分布为 4 颗湖心 BOSS 岛、5 颗池水外圈西岸 / 东岸水岸，避免后续位置调整导致存档错位。
- 每颗白莲只能激活一次；已激活白莲再次交互只显示已激活状态和当前进度，不扣除莲池仙露 / 灵韵，不重复给经验。
- 激活状态必须持久化；单颗经验奖励、最终仙体解锁和消耗扣除都需要防重复领取。
- 激活流程需要处理掉线 / 保存失败边界：参考仙体切换事务，先检查存档可用，再执行本地事务变更并调用 `SaveSystem.Save(callback)` 强保存；保存成功后确认激活，保存失败时回滚本次激活、消耗、经验和仙体解锁状态，避免未落盘状态悬在内存里。
- 9 颗全部激活时，只调用账号级仙体解锁，不自动切换当前角色；玩家后续仍通过仙体界面手动切换。
- 莲池仙露是新圣器级消耗品，已注册道具、图标、怪物掉落和黑市条目；暂不接入打包、锻造或其他后置系统。
- 青莲长生体作为新仙体，已接入 `AscensionConfig.GROWTH_PROFILES`、`ImmortalBodySystem` 账号级解锁、仙体面板卡片、属性预览、属性汇总和存档兼容。
- 仙体卡资源不是普通图标；已生成整张竖版仙体卡，包含青莲 / 长生意象剪影、青白卡背光效和底部“青莲长生体”艺术字，并加入仙体面板贴图映射。

存档与掉线口径：

| 场景 | 处理 |
|------|------|
| 正常激活 | 校验存档可用、白莲未激活、莲池仙露数量和灵韵；建立本地快照；扣除莲池仙露与灵韵；写入白莲激活状态；发放 1000万经验；若达到 9/9 则解锁账号级青莲长生体；随后强保存 |
| 重复交互 | 若该白莲已激活，只显示已激活和当前进度，不再扣消耗、不再给经验 |
| 连续点击 | 激活保存中锁定按钮 / 读条，防止同一白莲重复提交 |
| 保存失败 | 使用本地快照回滚白莲状态、背包、灵韵、经验 / 等级和账号仙体解锁缓存；提示失败，玩家可重新激活 |
| 保存成功前掉线或关闭 | 重新登录后以云存档为准；若激活未保存，则该白莲仍未激活，消耗和经验也不应持久化，玩家可重新激活 |
| 保存成功但回包丢失 | 重新登录后云存档已有激活状态，不能重复激活 |
| 9/9 完成 | 写入白莲进度和账号级仙体解锁；弹出获得展示；不切换当前角色仙体 |
| 已拥有仙体 | 账号已解锁青莲长生体时，9/9 完成只补齐本系统完成状态和展示提示，不重复创建账号资产 |

已新增存档字段：

```lua
qinglianBody = {
    version = 1,
    activatedLotuses = {
        ch6_qingbai_lotus_01 = true,
    },
    finalClaimed = false,
}
```

自测要求：

| 链路 | 自测重点 |
|------|----------|
| 属性 | `AscensionConfig.GROWTH_PROFILES.immortal_body_qinglian` 必须为生命 +30/级、生命恢复 +1/级、攻击 +3/级、防御 +2/级；解锁后仙体面板可预览该成长模板，但 9/9 完成时不自动切换当前仙体 |
| 消耗 | 每颗白莲成功激活只扣莲池仙露 ×1、灵韵 20000，并只发放 1000万经验一次 |
| 存档 | `qinglianBody` 写入 DTO、保存与读档链路；旧档字段缺失时按未激活处理；单颗激活状态与 9/9 最终领取状态都要持久化 |
| 回滚 | 强保存失败时回滚本次白莲状态、莲池仙露、灵韵、经验 / 等级和账号仙体解锁缓存 |
| 掉落 | 蛤蟆仙人掉落莲池仙露 0.2%；呱大人掉落莲池仙露 0.05%；莲池仙露进入黑市-消耗品，定价 60 / 30 |
| 地图 | 第六章只存在 9 颗稳定 ID 天青白莲，4 颗在 BOSS 岛、5 颗在池水外圈西岸 / 东岸水岸，全部为 `qinglian_body_lotus`，不得残留“暂未开放”旧交互 |

---

### 3.3 装备商人新增售卖口径（已实装）

适用对象：现有装备商人入口（`sell_equip` / `EquipShopUI`）。

| 项目 | 规则 |
|------|------|
| 新增售卖内容 | T1 全部紫色命格 |
| 属性口径 | 固定最低属性，不进行随机浮动 |
| 价格 | 每件 100 灵韵 |
| 货币类型 | 灵韵 |
| 入口 NPC | `ch6_merchant`，位于 `shadow_spawn_safe` `(73, 39)`，交互类型为 `sell_equip` |
| 状态 | 已实装，接入现有 `EquipShopUI` |

实现备注：

- 售卖对象为命格，不是装备；已沿用命格系统的数据结构、命格背包写入、属性显示和存档字段。
- “T1 全部紫色命格”覆盖 `MinggeData.SOURCES` 中 `tier = 1` 的全部 15 个命格来源。
- “固定最低属性”取 `MinggeData.STAT_RANGES[1][stat].purple[1]`，不使用常规掉落随机生成器。
- 购买消耗灵韵，走现有灵韵价格标签；命格背包满时不扣款，写入失败时回退扣款。
- 章节边界为第六章及之后；第六章装备商人商品列表独立构建，不继承前五章基础商品。
- 第六章地图已注册独立装备商人 `ch6_merchant`，不复用第一章 `equip_merchant`，避免章节 NPC 混淆。
- 代码落点：`scripts/ui/EquipShopUI.lua`；合同测试：`scripts/tests/test_equip_shop_mingge_contract.lua`。

---

### 3.4 两界阵石（已实装）

两界阵石位于中央两界村围墙外四角，使用同一套透明背景 PNG 石柱素材，由运行时根据属性类型和激活状态叠加不同颜色的阵纹、光晕与脉冲。四个对象使用稳定 ID，后续允许在地图冲突检查后微调坐标，但不得改 ID。

实现落点：`scripts/config/LiangjieStoneConfig.lua`、`scripts/systems/LiangjieStoneSystem.lua`、`scripts/ui/LiangjieStoneUI.lua`、`scripts/config/ZoneData_ch6.lua`、`scripts/rendering/entities/npcs.lua`、`scripts/ui/NPCDialog.lua`。PNG 为 `assets/image/ch6_liangjie_array_stone_20260710105731.png`。

| 阵石 | 内部 ID | 方向 | 属性 | 每级 | 30级满额 |
|------|---------|------|------|------|----------|
| 两界阵石·玄壁 | `xuanbi` | 西北 | 防御 | `+4` | `+120` |
| 两界阵石·天锋 | `tianfeng` | 东北 | 攻击 | `+5` | `+150` |
| 两界阵石·厚土 | `houtu` | 西南 | 生命上限 | `+50` | `+1500` |
| 两界阵石·回元 | `huiyuan` | 东南 | 生命回复 | `+5` | `+150` |

交互边界：

- 每根阵石独立保存 `activated`、`level` 与 `activationRewardClaimed`；激活后等级为 `0`，激活本身不提供属性。
- 每根阵石消耗 `1000` 谪仙令激活，最高 `30` 级。
- 每根阵石首次激活一次性获得 `1000万经验`，奖励规则与天青白莲一致；重复交互不得重复发放。
- 激活保存成功后弹出独立结果弹窗，明确显示经验已写入存档、本次实际经验增量，以及可能发生的等级变化。
- 未激活阵石首次交互只打开参考天青白莲的激活确认弹窗，显示消耗、持有量与经验奖励；取消不产生任何变化。
- 激活保存成功后，后续交互不再显示确认弹窗，直接打开常规养成面板。
- 升级价格沿用海神柱 / 祀剑池的 10 档令牌价格，每档覆盖 3 级；单石满级共消耗 `50500` 谪仙令，四石全满共消耗 `202000` 谪仙令。
- 四根阵石均为带 NPC 姓名板的对象：未激活显示 `未激活`，激活后显示 `Lv.N`，30 级显示 `MAX`。
- 激活后的光效强度固定，不按等级提高亮度、范围、粒子数或呼吸幅度；等级差异只通过姓名板与面板显示。
- 阵石从进入第六章起即可交互，不绑定 `shadow_village_zone` 完成状态；主线解封不会自动激活阵石。
- 阵石只提供永久平面属性，不承担传送、解封、召唤 BOSS 或开启副本功能。
- `liangjieStones.stones[*]` 是属性存档唯一真值；角色聚合字段必须在客户端读档和服务端写档时按规范等级重算，不能信任客户端派生值。
- 激活强保存失败时，必须回滚谪仙令、激活状态、奖励标记、经验/等级与四项派生属性。
- 完整数值、谪仙令产出、UI、存档和测试要求统一以 [第六章-令牌养成系统文档.md](第六章-令牌养成系统文档.md) 为准。

---

## 四、主线 NPC 功能

主线 NPC 负责发放、进度展示、奖励领取和章节完成标记。当前第六章主线按 8 步建立：

| 步骤 | 任务名 | 目标 | 奖励 |
|------|--------|------|------|
| 1 | 影门试锋 | 巡游天神·凌风 ×30，巡逻仙兵 ×100 | 仙1紫装 3选1 |
| 2 | 夜荒寻踪 | 夜游神·烛幽 ×30，影游使 ×100 | 一转仙劫丹 ×1，上品修炼果 ×1，金砖 ×1 |
| 3 | 断岳开山 | 两界山神·断岳 ×30，山岭巨像 ×100 | 仙1橙装 3选1 |
| 4 | 西营破阵 | 西大营天将·破军 ×30，西大营天将·镇垣 ×30 | 一转仙劫丹 ×1，上品修炼果 ×1，金砖 ×1 |
| 5 | 东营破阵 | 东大营天将·青锋 ×30，东大营天将·雷策 ×30 | 一转仙劫丹 ×1，上品修炼果 ×1，金砖 ×1 |
| 6 | 哼元压营 | 哼元帅 ×30，西营天兵 ×100 | 仙1青装 3选1 |
| 7 | 哈元镇营 | 哈元帅 ×30，东营天兵 ×100 | 仙1青装 3选1 |
| 8 | 村心解封 | 解开中央两界村封印 | 无；完成第六章主线标记 |

### 4.1 封印落地边界

| 封印 ID | 绑定任务链 | 作用 | 当前状态 |
|---------|------------|------|----------|
| `ch6_shadow_village` | `shadow_village_zone` | 中央两界村四门封印 | 第 8 步可解除，解除后恢复 `TOWN_ROAD` |
| `seal_ch6_tiger_trial` | `ch6_tiger_trial_future` | 虎王试炼未来副本入口 | `manualOnly = true`、`futureLocked = true`，当前没有任何解封方式 |

实现约束：

- `VillageChiefUI` 查找 `questChain = "shadow_village_zone"`，第 8 步调用 `QuestSystem.Unseal("ch6_shadow_village")`。
- `seal_ch6_tiger_trial.questChain` 不得绑定 `shadow_village_zone`。
- `QuestSystem` 对 `futureLocked` 封印执行拒绝解封、读档重锁、GM 常规切换阻断和地图阻挡层保底。
- 地图封印不只依赖瓦片不可通行，还会写入 `GameMap:SetSealBlockedTiles`，防止视觉或地形覆盖后穿过封印。

---

## 五、第五章任务数量同步

第六章主线设计参照记录：第五章主线中涉及 BOSS 击杀的目标数量统一调整为 20 个，涉及精英击杀的目标数量统一调整为 100 个。代码落点在 `scripts/config/QuestData_ch5.lua`。

---

## 六、待确认项

| 项目 | 当前状态 |
|------|----------|
| 主线 NPC 命名 | 已定为“村长” |
| 主线链 ID | 已定为 `shadow_village_zone` |
| 奖励物品 ID | 已落地：`one_turn_tribulation_pill`、`exp_pill_superior`、`gold_brick` |
| 仙1装备 3选1 | 已按 `tier = 11` 和品质过滤落地；后续如需固定部位池，可在装备生成池层补充 |
| 终局解锁 | 已定：第 8 步只解开中央两界村封印 |
| 青莲长生体 | 已实装 9 颗天青白莲、莲池仙露掉落 / 黑市、仙体属性、仙体卡片和存档链路 |
| 两界阵石 | 已实装四对象、指定坐标、PNG、交互路由、30 级属性、青莲式激活弹窗、固定光效、NPC 等级姓名板、单石 1000万激活经验、强保存回滚与服务端规范重算 |

---

## 七、迭代记录

| 日期 | 变更 |
|------|------|
| 2026-06-27 | 建立第六章 NPC 功能文档；记录初始远端基线、基础 NPC 功能槽位与炼丹炉口径 |
| 2026-07-01 | 落地村长主线 NPC、`shadow_village_zone` 八步主线、中央两界村封印、虎王试炼 futureLocked 边界；同步第五章 BOSS/精英任务数量口径 |
| 2026-07-01 | 确认青莲长生体获取规则：天青白莲调整为 9 颗激活，4 颗在 BOSS 岛、5 颗沿西岸 / 东岸池水外圈，消耗莲池仙露和灵韵，最终获得新仙体 |
| 2026-07-01 | 实装青莲长生体获取链路：9 颗天青白莲一次性激活、莲池仙露道具 / 掉落 / 黑市、仙体成长模板、仙体卡片、存档字段和保存失败回滚自测 |
| 2026-07-02 | 实装装备商人新增售卖口径：T1 全部紫色命格，固定最低属性，每件 100 灵韵，写入命格背包并加入合同测试 |
| 2026-07-02 | 补充第六章独立装备商人 `ch6_merchant`，位于影入口安全区，交互接入 `sell_equip` |
| 2026-07-10 | 定稿两界阵石：两界村外四角新增 4 根 PNG 阵法石柱，使用谪仙令激活并培养至 30 级，分别提供攻击 / 防御 / 生命 / 生命回复；详细规则拆入独立令牌养成文档 |
| 2026-07-10 | 二次定稿两界阵石：激活光效强度固定；NPC 姓名板显示 `未激活 / Lv.N / MAX`；单石首次激活奖励 1000万经验；回元改为 +5/级；强化规范存档、服务端重算和失败回滚要求 |
| 2026-07-10 | 三次定稿两界阵石：玄壁降为 +4防御/级、满级 +120；未激活首次交互使用青莲式确认弹窗，激活后再进入常规养成面板 |
| 2026-07-10 | 完成两界阵石四 NPC、PNG、交互面板、等级姓名板、属性与 v36 存档链路；复核海神柱 / 祀剑池后统一规范档案与属性重算规则，并加固保存超时回调和经验升级回滚 |
| 2026-07-14 | 第六章影入口安全区新增独立百宝箱 `warehouse_chest_ch6`，坐标 `(78, 37)`；复用前五章仓库素材、`warehouse` 交互、统一仓库 UI 与角色仓库存档，并纳入运行时部署审计和近身交互测试 |
