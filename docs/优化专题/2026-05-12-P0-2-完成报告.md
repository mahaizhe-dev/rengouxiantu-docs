# P0-2 完成报告

任务ID: P0-2
本次结论: 完成

## 1. Git 交付信息

- PR 链接: N/A（直接 commit 到 main）
- commit SHA: `40988f6`
- parent SHA: `64f2e21`
- diff 摘要（7 files changed, 646 insertions）:
  - 新增 `scripts/config/FeatureFlags.lua`（统一功能开关模块，183 行）
  - 新增 `scripts/tests/test_feature_flags.lua`（纯 Lua 测试，311 行，79 个断言）
  - 新增 `docs/优化专题/2026-05-12-P0-2-完成报告.md`（本报告，119 行）
  - 修改 `scripts/main.lua`（+10 行：require + 启动快照日志）
  - 修改 `scripts/server_main.lua`（+10 行：require + 启动快照日志）
  - 修改 `scripts/client_main.lua`（+1 行：require）
  - 修改 `scripts/tests/TestRegistry.lua`（+10 行：注册 feature_flags 测试为 blocking）

## 2. 新增/修改文件

- 新增:
  - `scripts/config/FeatureFlags.lua`
  - `scripts/tests/test_feature_flags.lua`
  - `docs/优化专题/2026-05-12-P0-2-完成报告.md`
- 修改:
  - `scripts/main.lua`
  - `scripts/server_main.lua`
  - `scripts/client_main.lua`
  - `scripts/tests/TestRegistry.lua`

## 3. 开关清单

| flag | 默认值 | 对应任务 | 接入点 | 说明 |
|------|--------|----------|--------|------|
| SAVE_PIPELINE_V2 | false | P0-5 | main.lua, server_main.lua | 启用 v2 存档管线（SavePersistence 统一读写） |
| SERVER_SAVE_VALIDATOR_V2 | false | P0-5 | server_main.lua | 启用 v2 服务端存档校验器 |
| SAVE_MIGRATION_SINGLE_ENTRY | false | P0-3 | main.lua, server_main.lua | 启用迁移单入口模式（所有迁移逻辑收口到 SaveMigrations） |
| NEW_MAIN_LOOP_ORCHESTRATOR | false | P0-4 | main.lua | 启用新版主循环编排器（替换手动调度） |
| NEW_SERVER_ROUTER | false | P0-6 | server_main.lua | 启用新版服务端路由器（替换 handler 分发） |
| CLOUDSTORAGE_STRICT_BATCHSET | false | P0-5 | server_main.lua | 启用 CloudStorage 严格 BatchSet 模式（禁止散写） |

## 4. 运行时接入

- 哪些文件读取了 FeatureFlags:
  - `scripts/main.lua` — require + GameStart() 启动快照日志
  - `scripts/server_main.lua` — require + Start() 启动快照日志
  - `scripts/client_main.lua` — require（通过 DoGameStart→main.lua 间接使用）
- 是否存在 require 循环: **否**
- 如何避免: FeatureFlags.lua 是纯叶模块，零依赖（不 require 任何业务模块）。它在 require 链的最上游，被入口文件最先加载。

## 5. 自动化测试

- 新增测试文件: `scripts/tests/test_feature_flags.lua`
- 注册到哪个 group: `config`
- gate 分类: `blocking`（阻断门禁）
- 运行命令: `python3 scripts/tests/_run_via_lupa.py --gate=blocking`
- 原始输出:

```
[TEST] feature_flags
--- FF TEST 1: FeatureFlags 可加载 ---
--- FF TEST 2: 6 个开关已注册 ---
--- FF TEST 3: 所有默认值为 false ---
--- FF TEST 4: 未定义开关安全降级 ---
--- FF TEST 5: isEnabled API ---
--- FF TEST 6: setOverride ---
--- FF TEST 7: clearOverride ---
--- FF TEST 8: clearAllOverrides ---
--- FF TEST 9: getMeta 完整性 ---
--- FF TEST 10: flag-task 映射 ---
--- FF TEST 11: snapshot ---
--- FF TEST 12: 关闭开关 → 旧路径 ---

============================================================
  FEATURE FLAGS: 79 tests, 79 passed, 0 failed
============================================================

  ALL FEATURE FLAGS TESTS PASSED

[PASS] feature_flags (79/79 passed)
```

- 全量阻断门禁结果:

```
[SUMMARY] total=5 executed=5 passed=5 failed=0 skipped=0
EXIT: SUCCESS
```

## 6. 兼容性说明

- 默认值下是否保持旧行为: **是**。所有 6 个开关默认 false，不触发任何新路径。
- 是否有任何行为变更: **否**。仅增加了启动时的日志输出（`[FeatureFlags] snapshot: ...`），运行时逻辑完全不变。
- 若有，具体是什么: N/A

## 7. 风险与回滚

- 已知风险:
  - 无。模块为纯叶节点，所有开关默认关闭，不影响任何业务逻辑。
- 回滚方式:
  1. 删除 `scripts/config/FeatureFlags.lua`
  2. 删除 `scripts/tests/test_feature_flags.lua`
  3. 从 main.lua / server_main.lua / client_main.lua 中移除 FeatureFlags 相关的 require 和日志代码
  4. 从 TestRegistry.lua 中移除 feature_flags 测试条目

## 8. 下一步建议

- 是否允许进入 P0-5: **是**（待复核方确认本报告 + Git 代码后）
- 后续任务可通过 `FeatureFlags.setOverride("FLAG_NAME", true)` 在运行时开启对应开关进行灰度验证
- 建议 P0-3（迁移收口）可与 P0-5 并行推进，因为 `SAVE_MIGRATION_SINGLE_ENTRY` 开关已就位

## 成功标准逐条验证

| # | 标准 | 状态 |
|---|------|------|
| 1 | 全项目只有一个统一的功能开关入口模块 | ✅ `scripts/config/FeatureFlags.lua` |
| 2 | 第一批高风险开关已定义，默认值不改变当前行为 | ✅ 6 个开关，全部默认 false |
| 3 | 代码读取开关通过统一 API（`FF.isEnabled()`） | ✅ 入口文件通过 `FeatureFlags.isEnabled()` 读取 |
| 4 | 至少一组纯 Lua 自动化测试验证开关默认值和读取行为 | ✅ 79 个断言，12 个测试组 |
| 5 | 每个开关写清楚对应哪个后续任务 | ✅ 元数据 `task` 字段：P0-3/P0-4/P0-5/P0-6 |
