# R3 — 服务端主存档链路等价拆分第一阶段 完成报告

> 任务卡：`docs/2026-05-13-R3-服务端主存档链路等价拆分第一阶段任务卡.md`
> 执行日期：2026-05-13

---

## 1. commit SHA / parent SHA

| 项目 | 值 |
|------|-----|
| parent SHA | `3319797` (docs(BM-01A): 修正完成报告 commit SHA 为远端实际值) |
| commit SHA | `5c2180a` |

---

## 2. 实际修改文件清单

| 文件 | 操作 | 行数 | 说明 |
|------|------|------|------|
| `scripts/server_main.lua` | **修改** | 1042 行（原 ~1900 行） | 三个 Handler 替换为薄壳委托 |
| `scripts/network/SlotReadService.lua` | **新增** | 285 行 | HandleFetchSlots 逻辑平移 |
| `scripts/network/SaveLoadService.lua` | **新增** | 204 行 | HandleLoadGame 逻辑平移 |
| `scripts/network/SaveWriteService.lua` | **新增** | 534 行 | HandleSaveGame 逻辑平移 |
| `scripts/tests/test_r3_service_shell_contract.lua` | **新增** | 325 行 | R3 契约测试（40 项断言） |
| `scripts/tests/TestRegistry.lua` | **修改** | +8 行 | 注册 R3 测试为 blocking gate |

**禁写文件确认**：以下文件均 **未修改**：

- `scripts/network/SaveProtocol.lua` ✅
- `scripts/network/BlackMerchantHandler.lua` ✅
- `scripts/network/CloudStorage.lua` ✅
- `scripts/network/SaveSystemNet.lua` ✅
- `scripts/systems/save/*` ✅
- `scripts/main.lua` / `scripts/client_main.lua` ✅
- `scripts/config/FeatureFlags.lua` / `scripts/network/MigrationPolicy.lua` ✅

---

## 3. server_main.lua → service 调用关系对照表

| server_main.lua Handler | 委托目标 | 传入参数 | server_main 保留逻辑 |
|------------------------|---------|---------|-------------------|
| `HandleFetchSlots` | `SlotReadService.Execute(connection, connKey, userId)` | connection, connKey, userId | connKey/userId 提取 + 鉴权 |
| `HandleLoadGame` | `SaveLoadService.Execute(connection, connKey, userId, slot)` | connection, connKey, userId, slot | connKey/userId 提取 + 鉴权 + `connSlots_[connKey] = slot` |
| `HandleSaveGame` | `SaveWriteService.Execute(connection, connKey, userId, slot, eventData)` | connection, connKey, userId, slot, eventData | connKey/userId 提取 + 鉴权 + `connSlots_[connKey] = slot` |

**未拆分的 Handler**（保留在 server_main.lua）：

| Handler | 原因 |
|---------|------|
| `HandleCreateChar` | 非主存档链路，创角逻辑独立 |
| `HandleDeleteChar` | 非主存档链路，删角逻辑独立 |
| `HandleMigrateData` | 迁移链路独立，且与 MigrationPolicy 耦合 |
| `HandleSkinPurchase` | 已拆至 SkinShopHandler |

---

## 4. 主存档正文 / 账号级旁路 / 排行旁路 / 黑市排除项 对照表

### SlotReadService (HandleFetchSlots)

| 分区标记 | 内容 | key 读写 |
|---------|------|---------|
| `[主存档正文]` | BatchGet save_{slot} 读取各存档槽 | 读 `save_1`~`save_3` |
| `[账号级旁路]` | 读 slots_index、account_atlas、account_cosmetics | 读 `slots_index`, `account_atlas`, `account_cosmetics` |
| `[排行旁路]` | 读排行榜 score/info 用于回传 | 读 `rank_score_*`, `rank_info_*`, `rank2_score`, `rank2_info` |
| `[管理员一次性操作]` | admin 标记的存档迁移 | 读写 `admin_migrate_*` |

### SaveLoadService (HandleLoadGame)

| 分区标记 | 内容 | key 读写 |
|---------|------|---------|
| `[主存档正文]` | BatchGet save_{slot} 加载存档 | 读 `save_{slot}` |
| `[账号级旁路]` | 读 account_atlas、account_cosmetics、login_{slot} | 读 `account_atlas`, `account_cosmetics`, `login_{slot}` |
| `[过渡代码]` | premigrate 预迁移数据检测 | 读 `premigrate_{slot}` |
| `[BM 边界冻结]` | CheckWALOnLogin 调用（仅调用 BM 已有接口） | 无直接 key 读写 |

### SaveWriteService (HandleSaveGame)

| 分区标记 | 内容 | key 读写 |
|---------|------|---------|
| `[主存档正文]` | 核心存档验证 + batch:Save("存档") | 写 `save_{slot}` |
| `[账号级旁路]` | account_atlas、account_cosmetics | 写 `account_atlas`, `account_cosmetics` |
| `[排行旁路]` | rank score/info 更新 | 写 `rank_score_*`, `rank_info_*`, `rank2_score`, `rank2_info` |
| `[试炼旁路]` | 试炼塔排行 | 写 `trialTower_rank_*` |
| `[镇狱塔旁路]` | 镇狱塔排行 | 写 `prisonTower_rank_*` |
| `[版本守卫]` | CURRENT_SAVE_VERSION 校验 | 无独立 key |
| `[B4-S2 硬校验]` | _ValidateCoreData 字段完整性 | 无独立 key |

---

## 5. 黑市一致性影响评估

### §12 要求的 4 项评估

| # | 评估项 | 结论 |
|---|--------|------|
| 1 | 本次是否修改了 `BlackMerchantHandler.lua` | **否** — 文件不在写集 |
| 2 | 本次是否修改了 BM 协议或 BM key | **否** — 未改动任何 BM 相关协议或 key |
| 3 | 本次是否改变了 `BM-01A` 依赖的 `save_request → SaveGame 落盘` 语义 | **否** — `SaveWriteService.Execute` 保持原始 `batch:Save("存档")` + `S2C_SaveResult ok=true` 语义链完全不变 |
| 4 | 本次是否可能让黑市相关旁路数据被错误吞入 service 主链 | **否** — BM WAL/history/exchange/recycle 均不在任何 Service 的 require 或逻辑中 |

### §6.3 黑市接触面盘点

| 黑市相关项 | 是否在本次写集 | 说明 |
|-----------|--------------|------|
| `BlackMerchantHandler.lua` | ❌ 不在 | 仅 SaveLoadService 引用其 `CheckWALOnLogin`（原有行为） |
| WAL 机制 | ❌ 不在 | 由 BM Handler 独立管理 |
| `bm_history` / `bm_exchange` / `bm_recycle` | ❌ 不在 | 这些 key 不出现在任何 Service 模块 |
| `WriteSaveBack` | ❌ 不在 | SaveWriteService 不调用此函数 |
| `save_request → 服务端落盘` 语义 | ✅ 等价保持 | `batch:Save("存档")` → `S2C_SaveResult ok=true` 链路完全平移 |

---

## 6. 原始测试输出

### blocking gate (13/13 通过)

```
[SUMMARY] total=13 executed=13 passed=13 failed=0 skipped=0
EXIT: SUCCESS
```

子测试明细：

| 测试套件 | 通过 |
|---------|------|
| runner_smoke | 157/157 |
| save_optimization | 81/81 |
| prison_tower | 450/450 |
| recycle_system | 115/115 |
| feature_flags | 81/81 |
| cloudstorage_network_semantics | 25/25 |
| save_dto_contract | 68/68 |
| migration_state_policy | 55/55 |
| logger_smoke | 40/40 |
| save_system_net_state_machine | 34/34 |
| bm_warehouse_consistency | 19/19 |
| bm_warehouse_consistency_01a | 13/13 |
| **r3_service_shell_contract** | **40/40** |

**总计：1168 项子测试全绿**

### full suite (--all, 27/27 通过)

```
[SUMMARY] total=27 executed=13 passed=13 failed=0 skipped=14
EXIT: SUCCESS
```

14 项 skipped 均为 `engine_required`（需引擎运行时），与 R3 无关。

### R3 契约测试详细 (40/40)

```
§1: Service 模块加载 — 3 ✓
§2: server_main.lua 薄壳委托 — 6 ✓
§3: BM 排除断言 — 5 ✓
§4: 分区注释标记 (R3 §8.3) — 11 ✓
§5: server_main.lua require 验证 — 3 ✓
§6: BM-01A 依赖链 — 3 ✓
§7: 禁写文件排除 — 8 ✓
§8: 代码瘦身验证 — 1 ✓
```

---

## 7. rules.md 符合性说明

| rules.md 条款 | 符合性 |
|--------------|--------|
| §1 构建命令 | ✅ 使用 `entry_client="client_main.lua", entry_server="server_main.lua"` |
| §4.6.1 fillEquipFields | ✅ 未修改 |
| §5.2 删除文件引用检查 | ✅ 未删除任何文件 |
| §6 GM_ENABLED | ✅ 未修改，保持 false |
| §9 存档版本 | ✅ CURRENT_SAVE_VERSION = 23，未修改 |

---

## 8. 是否允许进入 R3-G1

**结论：完成，可进入 R3-G1**

理由：
1. 三个 Handler 已等价拆分为独立 Service 模块，`server_main.lua` 从 ~1900 行降至 1042 行
2. 分区标记（§8.3）已全部到位：`[主存档正文]`、`[账号级旁路]`、`[排行旁路]`、`[试炼旁路]`、`[镇狱塔旁路]`、`[BM 边界冻结]`、`[过渡代码]`、`[版本守卫]`、`[B4-S2 硬校验]`
3. 黑市边界完全冻结，BM-01A 语义链未受影响
4. 40 项契约测试 + 1168 项全量门禁子测试全绿
5. 未触碰任何禁写文件
6. Build 通过，构建产物 entry 正确

### 手工验证建议（上线前）

1. 角色列表加载正常
2. 进入已有角色正常
3. 手动保存后退出重入数据一致
4. 新建测试角色正常
5. 删除测试角色正常
6. 黑市打开、查询、普通卖出路径无异常

---

*报告生成时间：2026-05-13*
