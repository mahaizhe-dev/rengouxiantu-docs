# P0-3 完成报告

任务ID: P0-3
本次结论: 完成

---

## 1. Git 交付信息

- PR 链接: N/A（直接 push 到 main）
- commit SHA: `3336007`
- parent SHA: `5d0de8a`
- diff 摘要: 7 files changed, 655 insertions(+), 21 deletions(-)
  - `scripts/systems/save/SaveDTO.lua` — 新增 225 行，28 schema + 3 passthrough 字段边界约束
  - `scripts/systems/save/SavePostLoadSync.lua` — 新增 60 行，抽离低歧义加载后同步逻辑
  - `scripts/systems/save/SavePersistence.lua` — +16 行，SAVE_PIPELINE_V2 影子校验接入
  - `scripts/systems/save/SaveLoader.lua` — +25/-21 行，SAVE_PIPELINE_V2 新路径接入
  - `scripts/tests/test_save_dto_contract.lua` — 新增 313 行，58 条用例
  - `scripts/tests/TestRegistry.lua` — +10 行，注册 save_dto_contract 到 blocking gate
  - `docs/架构文档/rules.md` — +3/-3 行，常量校正

---

## 2. SaveDTO 实现

- **导出接口**: `FromCoreData`, `ToCoreData`, `ValidateBoundary`, `GetSchemaFields`, `GetSchemaFieldCount`, `IsSchemaField`, `GetPassthroughFields`, `GetPassthroughFieldCount`, `IsPassthroughField`
- **明确纳入 DTO 的字段域**: 28 个 SCHEMA_FIELDS（P0-3A 合同 22 个 + DoSave 额外 6 个）
- **明确排除的字段域**: account_atlas, account_bulletin, account_cosmetics, slots_index, login_*, prev_*, deleted_*, rank*, player_level, premigrate_*, inventory 等
- **透传字段处理**: atlas, accountCosmetics, pendingXianyuanRewards 单独存储在 PASSTHROUGH_FIELDS，FromCoreData 分离返回，ToCoreData 合并写回，DTO 不解析其内容

---

## 3. SavePostLoadSync 抽离结果

- **实际移动的代码块**:
  1. `AtlasSystem.PostLoadSync()` 调用（从 ProcessLoadedData 开头）
  2. HP recalibration 逻辑（玩家满血修正）
- **明确保留在旧路径的代码块**:
  1. `TitleBuffSystem.RecalcAllTitleBuffs()` — 依赖全局 player 状态，动则风险高
  2. `RecycleSystem.MigrateOldFormat()` — 迁移逻辑，不应拆到同步模块
  3. Equipment recalc 整块 — 涉及装备/套装/属性联动，歧义度高
  4. SaveMigrations 调用 — 迁移链必须保留在 SaveLoader 中
- **为什么不能现在动**: 高歧义代码块涉及多系统耦合，等价验证困难，需后续 P0-4 逐步处理

---

## 4. Feature Flag 接入

- **SAVE_PIPELINE_V2=false 时行为**: 线上默认路径，零变化。SaveDTO/SavePostLoadSync 不参与任何运行时逻辑
- **SAVE_PIPELINE_V2=true 时行为**: 
  - SavePersistence.DoSave 后执行 `SaveDTO.ValidateBoundary()` 影子校验（仅 warn 日志，不拦截保存）
  - SaveLoader.ProcessLoadedData 走 `SavePostLoadSync.Run()` 新路径（仅低歧义代码块）
- **是否真正切流**: 否，是影子包装。默认 false，开启后也只是 warn + 新路径并行验证

---

## 5. 版本与迁移

- **是否改了 CURRENT_SAVE_VERSION**: 否，保持 23
- **是否新增 MIGRATIONS**: 否
- **原因**: 等价重构，不改 coreData 结构，不改 key 名，不改落盘格式。SaveDTO 是纯读取/校验层，不产生新版本

---

## 6. 自动化测试

- **新增测试**: `test_save_dto_contract.lua`（58 条用例）
- **注册到**: `blocking` gate, `save` group（TestRegistry.lua id=`save_dto_contract`）
- **运行命令**: `python3 scripts/tests/_run_via_lupa.py scripts/tests/run_all.lua`
- **原始输出**:

```
[TestRunner] === 门禁测试报告 ===
[TestRunner] 环境: lupa (resty-lupa via _run_via_lupa.py)
[TestRunner] 门禁类型: blocking

[TestRunner] --- 执行 blocking 测试 ---

[TestRunner] [1/7] runner_smoke (scripts/tests/test_runner_smoke.lua)
[TestRunner] PASS: runner_smoke — 6 passed, 0 failed (total 6)

[TestRunner] [2/7] save_optimization (scripts/tests/test_save_optimization.lua)
[TestRunner] PASS: save_optimization — 180 passed, 0 failed (total 180)

[TestRunner] [3/7] prison_tower (scripts/tests/test_prison_tower.lua)
[TestRunner] PASS: prison_tower — 52 passed, 0 failed (total 52)

[TestRunner] [4/7] recycle_system (scripts/tests/test_recycle_system.lua)
[TestRunner] PASS: recycle_system — 88 passed, 0 failed (total 88)

[TestRunner] [5/7] feature_flags (scripts/tests/test_feature_flags.lua)
[TestRunner] PASS: feature_flags — 48 passed, 0 failed (total 48)

[TestRunner] [6/7] cloudstorage_network_semantics (scripts/tests/test_cloudstorage_network_semantics.lua)
[TestRunner] PASS: cloudstorage_network_semantics — 496 passed, 0 failed (total 496)

[TestRunner] [7/7] save_dto_contract (scripts/tests/test_save_dto_contract.lua)
[TestRunner] PASS: save_dto_contract — 58 passed, 0 failed (total 58)

[TestRunner] === 门禁结果 ===
[TestRunner] blocking: 7/7 通过
[TestRunner] 总断言: 928 passed, 0 failed
[TestRunner] 门禁状态: ✅ 全部通过
```

---

## 7. rules.md 对照检查

- **是否改动旧字段名**: 否
- **是否改动旧 key 名**: 否
- **是否删除旧迁移**: 否
- **是否存在必须整体同步的配对字段**: 否（SaveDTO 只读取/校验，不修改配对关系）
- **rules.md 常量区是否已校正**: 是。已校正 CURRENT_SAVE_VERSION=23, AUTO_SAVE_INTERVAL=120

---

## 8. 风险与回滚

- **已知残余歧义**: 
  1. SCHEMA_FIELDS 混合了 22 个 P0-3A 合同 DTO 主体字段 + 6 个 coreData 信封字段，未做三层分离 → **P0-3R 已修复**
  2. 高歧义代码块（装备重算、称号 buff、迁移逻辑）保留在 SaveLoader，需 P0-4 逐步处理
- **回滚方式**: `git revert 3336007`，默认 SAVE_PIPELINE_V2=false 确保回滚无副作用
- **是否允许进入下一步**: 是（P0-3R 三层收口完成后）
