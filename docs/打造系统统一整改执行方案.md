# 打造系统统一整改执行方案

> 基线：以当前代码为准，经代码审查确认后修正。
>
> 目标：把第四章、第五章打造系统整理成可维护、可扩展、可测试、可上线的统一系统。
>
> 原则：先修错，再统一；先保上线，再补扩展；先堵危险测试入口，再谈功能迭代。

---

## 一、最终目标

本次执行完成后，必须同时满足：

1. 第四章、第五章打造行为可用且规则明确。
2. 第四章随机套装灵器只会产出制式套装。
3. 第五章当前错误逻辑全部回到设计目标。
4. 新增第五章 `T10` 随机灵器打造。
5. 现有 GM 后台不再提供错误或危险测试入口。
6. 测试体系分层完成，线上进程不再执行带副作用的 forge 测试。
7. 系统具备后续继续扩配方、扩站点、扩随机池的能力。

---

## 二、明确边界

### 本次必须完成

1. ~~修复 `CreateSaintEquipment()` 中道藏圣衣/圣氅的属性错误。~~ — 设计确认：圣衣/圣氅统一圣性，当前代码正确。
2. 修复第四章随机套装池错误。
3. 建立统一打造配置模型。
4. 建立统一执行入口。
5. 接入新 `T10` 随机灵器打造。
6. 重构测试与 GM 调试边界。

### 本次不做

1. 不重写整个 UI 外观。
2. 不顺手重构所有旧掉落逻辑。
3. 不修改无关章节的装备生成系统。
4. 不把离线 mock 测试强行改成真实引擎自动测试。
5. **不追溯迁移老玩家已打造的装备**（老装备不动，仅新打造走修正后逻辑）。

---

## 三、现状问题清单

### P0. 测试体系边界错误

现状：

- `test_jiefeng_forge_pipeline.lua`
- `test_sword_forge_comprehensive.lua`

已经不再顶层自动污染，但执行时仍然会：

- 改 `package.loaded`
- 注入 `InventorySystem.SetManager(mock)`

结论：

- 这两份测试必须永久保留为 `lupa_only`
- 绝不允许在 GM / 真实引擎进程中执行

### ~~P1. 第五章 `CreateSaintEquipment()` 属性赋值~~ — 已确认正确

> **2026-06-02 设计确认**：圣衣、圣氅统一使用圣性（saintStat），与解封古剑、帝尊圣戒一致。
> 当前 `CreateSaintEquipment()` else 分支"清除 spiritStat → 生成随机 saintStat"即为正确行为，**无需修改**。

> **代码审查确认以下行为正确，无需修改：**
> - `CreateSaintEquipment()` else 分支：清空 spiritStat，随机生成 saintStat ✅
> - `CreateJiefengSword()`：清空 spiritStat，随机生成 saintStat ✅
> - `SwordForgeUI.DoForgeSword()` 原地变换：覆盖 spiritStat = nil，保留 forgeStat ✅
> - `CreateLonghunling()`：走法宝生成器，不注入 saintStat ✅

设计目标（最终确认）：

| 装备 | spiritStat | saintStat | 当前状态 |
|------|:---:|:---:|------|
| `dizun_saint_ring` | ❌ 无 | ✅ 固定值 | ✅ 正确 |
| `daozang_saint_armor` | ❌ 无 | ✅ 随机 | ✅ 正确 |
| `saint_cape_ch5` | ❌ 无 | ✅ 随机 | ✅ 正确 |
| `jiefeng_*`（解封古剑） | ❌ 无 | ✅ 随机 | ✅ 正确 |
| `fabao_longhunling` | 走法宝链路 | ❌ 无 | ✅ 正确 |

**结论：所有圣性装备统一走 saintStat，当前代码全部正确，阶段 1 无需代码改动。**

### P1. 第四章随机套装池错误

现状：

- `LootSystem.ForgeRandomSetLingqi()` 直接扫 `EquipmentData.SetBonuses`（8 个 key）

结果：

- 会把非制式套装（tiger_king, wu_rings, humai_yizhuang, zhenzong_xuanwei）也抽出来
- 实际只应产出 4 套制式套装

### P1. 执行层分叉严重

现状：

- 第四章靠 `DragonForgeUI`
- 第五章靠 `SwordForgeUI`
- 规则、扣费、生成器、结果写回分散在两个 UI 中

后果：

- 新功能接入成本高
- 测试覆盖分裂
- 错误容易重复制造

### P2. 提交流程非原子

现状：

- 先扣金币
- 先删材料/装备
- 再生成产物
- 再写入背包/装备栏

后果：

- 生成失败会吞资源（概率极低，但工程规范应修复）

---

## 四、执行总顺序

执行顺序必须固定，不允许跳步：

1. 阶段 0：止血
2. 阶段 1：修第五章 CreateSaintEquipment
3. 阶段 2：修第四章随机套装池
4. 阶段 3：统一打造配置与执行器
5. 阶段 4：新增第五章 `T10` 随机灵器打造
6. 阶段 5：重构测试与 GM 调试
7. 阶段 6：完整回归与上线验收

---

## 五、阶段 0：止血

### 目标

先阻止错误继续扩散，避免测试和 GM 继续制造假结果。

### 任务

1. 保持 `TestRegistry.lua` 中以下条目为禁用状态：
   - `jiefeng_forge_pipeline`
   - `sword_forge_comprehensive`

2. 保持 GM 中以下行为为禁止提示，不恢复直造：
   - `CreateLonghunling(nil)`
   - `CreateJiefengSword(nil, ...)`

3. 新增一个 `blocking` 的 forge 静态合同测试，覆盖：
   - 配方存在性
   - 模板约束
   - GM 安全边界
   - Registry 安全边界

### 涉及文件

- `scripts/tests/TestRegistry.lua`
- `scripts/tests/test_forge_contract.lua`
- `scripts/ui/GMConsole.lua`

### 完成标准

1. 真实进程无法执行危险 forge mock 测试。
2. GM 无法再伪造无源龙魂令/解封古剑。
3. `blocking` 层具备最小 forge 合同门禁。

---

## 六、阶段 1：~~修第五章 CreateSaintEquipment~~ — 无需改动（已确认正确）

> **2026-06-02 设计确认**：圣衣、圣氅、解封古剑、帝尊圣戒统一使用圣性（saintStat）。
> `CreateSaintEquipment()` 当前 if/else 两路分支就是正确行为，**本阶段无需任何代码改动**。

### 确认结论

| 函数/路径 | 行为 | 结论 |
|-----------|------|------|
| `CreateSaintEquipment()` 路线 A | 保留固定 saintStat，清除 spiritStat | ✅ 正确 |
| `CreateSaintEquipment()` 路线 B（else） | 清除 spiritStat，随机生成 saintStat | ✅ 正确 |
| `CreateJiefengSword()` | 清除 spiritStat，随机生成 saintStat | ✅ 正确 |
| `SwordForgeUI.DoForgeSword()` 原地变换 | 覆盖 spiritStat=nil，保留 forgeStat | ✅ 正确 |
| `CreateLonghunling()` | 走法宝生成器，不注入 saintStat | ✅ 正确 |

### 涉及文件

无。

### 完成标准

确认代码审查通过即可，无需代码改动。

---

## 七、阶段 2：修第四章随机套装池

### 目标

让第四章随机套装灵器只出制式套装。

### 任务

1. 新增套装白名单池配置，例如：

```lua
EquipmentData.FORGE_SET_POOLS = {
    lingqi_t9_standard = { "xuesha", "qingyun", "fengmo", "haoqi" },
}
```

2. 修改 `LootSystem.ForgeRandomSetLingqi()`：
   - 不再遍历全 `SetBonuses`
   - 改为读取 `lingqi_t9_standard`

3. 保留 `SetBonuses` 作为总定义表，不再承担"随机池"职责。

### 涉及文件

- `scripts/config/EquipmentData.lua`
- `scripts/systems/LootSystem.lua`

### 完成标准

1. 第四章套装灵器只会出：
   - `xuesha`
   - `qingyun`
   - `fengmo`
   - `haoqi`
2. 非制式套装不再从随机锻造产出。

---

## 八、阶段 3：统一打造配置与执行器

### 目标

把第四章、第五章从"两套 UI + 两套执行逻辑"收束成"一套配方模型 + 一套执行器"。

### 任务 1：建立统一配置

新增统一配置结构：

```lua
EquipmentData.FORGE_STATIONS = {}
EquipmentData.FORGE_SET_POOLS = {}
EquipmentData.FORGE_RECIPES = {}
EquipmentData.FORGE_RECIPE_ORDER = {}
```

### 任务 2：建立统一执行器

新增：

- `scripts/systems/ForgeSystem.lua`

职责：

1. 读取统一配方
2. 校验金币、材料、背包、输入装备
3. 根据 `generator.type` 调生成器
4. 根据 `outputMode` 写回：
   - 装备栏原地替换
   - 背包新增
5. 统一发：
   - `save_request`
   - `equipment_changed`
6. **原子性保证**：先生成产物，成功后再扣费

### 任务 3：UI 降级为展示层

`DragonForgeUI.lua` 和 `SwordForgeUI.lua` 只保留：

1. 配方列表展示
2. 缺料提示
3. 调 `ForgeSystem.Execute(recipeId)`
4. 成功弹窗与刷新

不再各自维护完整执行链路。

### 任务 4：统一生成器协议

把当前生成逻辑统一成参数化调用，例如：

```lua
LootSystem.CreateRandomLingqi(spec)
LootSystem.CreateRandomSetLingqi(spec)
LootSystem.CreateSetEquipment(spec)
```

### 涉及文件

- `scripts/config/EquipmentData.lua`
- `scripts/systems/ForgeSystem.lua`
- `scripts/systems/LootSystem.lua`
- `scripts/ui/DragonForgeUI.lua`
- `scripts/ui/SwordForgeUI.lua`

### 完成标准

1. 新配方无需再复制新 UI 执行逻辑。
2. 第四章、第五章都可接到统一执行器。
3. 套装池、输入模式、输出模式都可配置化。

---

## 九、阶段 4：新增第五章 T10 随机灵器打造

### 目标

把第五章随机灵器打造接入地炉系统。

### 规则

1. 站点：`sword_forge`
2. 消耗：
   - `immortal_essence_blood x4`
   - `1000000` 金币
3. 产物：
   - `T10`
   - `quality = cyan`
4. 概率：
   - `70%` 普通 `T10` 随机灵器
   - `30%` 制式套装 `T10` 随机灵器
5. 制式套装池：
   - `xuesha`
   - `qingyun`
   - `fengmo`
   - `haoqi`

### 实现要求

1. 不新增硬编码专用函数名如 `ForgeRandomLingqiT10Ch5()`
2. 必须走统一生成器参数化方案
3. 必须走统一 `ForgeSystem`
4. 必须是背包模式，不是装备栏替换模式

### 建议配方定义

```lua
t10_random_lingqi_ch5 = {
    stationId = "sword_forge",
    label = "T10随机灵器",
    inputMode = "bag_forge",
    outputMode = "add_to_bag",
    gold = 1000000,
    consumables = {
        { id = "immortal_essence_blood", count = 4 },
    },
    generator = {
        type = "random_lingqi_mixed",
        tier = 10,
        quality = "cyan",
        setChance = 0.30,
        setPoolId = "lingqi_t10_standard",
    },
}
```

### 涉及文件

- `scripts/config/EquipmentData.lua`
- `scripts/systems/LootSystem.lua`
- `scripts/systems/ForgeSystem.lua`
- `scripts/ui/SwordForgeUI.lua`

### 完成标准

1. 地炉可打造 `T10` 随机灵器。
2. `30%` 套装，且只出四套制式。
3. 产物入背包，存档正常。

---

## 十、阶段 5：重构测试与 GM 调试

### 目标

让测试结构和执行方式与系统架构一致。

### 任务 1：保留三层测试体系

#### A层：静态合同测试

放进 `blocking`：

- `test_forge_contract.lua`
- `test_jiefeng_sword_config.lua`

#### B层：离线集成测试

继续 `skip`：

- `test_jiefeng_forge_pipeline.lua`
- `test_sword_forge_comprehensive.lua`

要求继续标记为：

- `enabled = false`
- `mode = "skip"`
- `gate = "non_blocking"`

#### C层：真实引擎人工验收

只通过真实 UI + GM 发基础材料执行。

### 任务 2：GM 分级

GM 后台只允许：

1. 发材料
2. 发基础输入装备
3. 发概率统计工具
4. 真实配方前置准备

GM 后台不允许：

1. 直造龙魂令
2. 直造解封古剑
3. 直跑 mock forge 大用例

### 任务 3：测试断言统一到设计目标

现有 forge 离线测试必须按以下规则校验：

1. 圣衣/披风：随机 `saintStat`，无 `spiritStat`
2. 帝尊圣戒：固定 `saintStat`，无 `spiritStat`
3. 解封古剑：随机 `saintStat`，无 `spiritStat`
4. 龙魂令：走法宝链路，无 `saintStat`

### 涉及文件

- `scripts/tests/TestRegistry.lua`
- `scripts/tests/test_forge_contract.lua`
- `scripts/tests/test_jiefeng_forge_pipeline.lua`
- `scripts/tests/test_sword_forge_comprehensive.lua`
- `scripts/ui/GMConsole.lua`

### 完成标准

1. 测试分层明确。
2. GM 不再误导流程。
3. 离线 forge 测试不会再给错误实现放绿灯。

---

## 十一、阶段 6：完整回归与上线验收

### T0. 静态门禁

每次提交必须检查：

1. `GMConsole.lua` 无 forge mock 入口
2. `TestRegistry.lua` 中 forge 离线大用例仍是 `skip`
3. `ForgeSystem` 不引入新的非原子扣费
4. 第四章/第五章随机套装池都走白名单

### T1. Blocking 测试

必须全绿：

1. `test_forge_contract.lua`
2. `test_jiefeng_sword_config.lua`

### T2. Lupa 离线集成测试

必须全绿：

1. `test_jiefeng_forge_pipeline.lua`
2. `test_sword_forge_comprehensive.lua`

### T3. 真实引擎验收

#### 第四章

1. 龙极令打造
2. T9 普通灵器
3. T9 套装灵器

#### 第五章

1. 帝尊圣戒（验证产物为固定 saintStat）
2. 道藏圣衣（验证产物为随机 saintStat，无 spiritStat）
3. 圣器披风（验证产物为随机 saintStat，无 spiritStat）
4. 龙魂令
5. 四把解封古剑（验证产物为随机 saintStat，无 spiritStat）
6. T10 随机灵器

#### 异常分支

1. 金币不足
2. 材料不足
3. 背包满
4. 输入装备不存在
5. 装备栏武器不正确

#### 存档验证

1. 打造后立即保存
2. 退出重进后属性还在
3. `spiritStat / saintStat / forgeStat` 不丢

### 上线阻断条件

以下任一存在，禁止上线：

1. 新打造的圣衣/披风/解封古剑缺少 `saintStat` 或残留 `spiritStat`
2. 提交流程仍吞资源
3. 随机套装仍扫全 `SetBonuses`
4. GM 仍可直造龙魂令/解封古剑

---

## 十二、实施节奏建议

建议按两个提交批次执行：

### 批次 A：修错与止血（P0 优先）

包含：

1. ~~阶段 1~~：代码审查确认 `CreateSaintEquipment()` 已正确（无需改动）
2. 阶段 2：修套装池白名单（`FORGE_SET_POOLS` + `ForgeRandomSetLingqi` 改读白名单）
3. 阶段 0：测试门禁与 GM 限制确认

目标：

- 确认第五章属性赋值逻辑正确，修正第四章随机池错误
- **实际代码改动仅阶段 2，风险极小**

### 批次 B：统一与扩展

包含：

1. 阶段 3：`ForgeSystem` 统一执行器
2. 阶段 4：第五章 T10 随机灵器
3. 阶段 5：测试体系重构

目标：

- 再把系统从"能用"提升到"可扩展"

不建议一口气混在一个超大提交里完成。

---

## 十三、执行 AI 约束

执行过程中必须遵守：

1. 不得把危险 forge 大用例重新接回 GM。
2. 不得为了测试方便恢复无源直造按钮。
3. 不得先加 `T10` 随机灵器再回头修旧逻辑。
4. 不得跳过真实引擎人工验收就判定可上线。
5. 不得追溯修改老玩家已打造的装备存档。
6. 每完成一个阶段，都要同步：
   - 代码变更点
   - 测试结果
   - 未解决风险

---

## 十四、最终交付物

本次执行完成后，必须交付：

1. 修正后的生产代码（CreateSaintEquipment + 套装池白名单）
2. 统一 forge 配置结构
3. `ForgeSystem.lua`
4. 第五章 `T10` 随机灵器打造
5. 更新后的 GM 调试边界
6. 分层测试脚本
7. 一次完整的上线验收结果

这 7 项缺一项，都不算执行完成。

---

## 附录：代码审查记录

> 以下为 2026-06-02 代码审查结论，作为本方案的事实依据。

### A. CreateSaintEquipment (generation.lua:653)

```
流程：CreateSpecialEquipment() 检测 template.hasSpiritStat → 生成 spiritStat
     → CreateSaintEquipment() else 分支 → 清除 spiritStat → 生成 saintStat
结论：✅ 正确（2026-06-02 设计确认：圣衣/圣氅统一走圣性 saintStat，不保留 spiritStat）
     原始审计报告中标记为 BUG 系设计目标误判，实际代码行为正确。
```

### B. CreateJiefengSword (LootSystem.lua:883)

```
行为：item.spiritStat = nil, 随机生成 saintStat
结论：✅ 正确（设计确认：解封古剑只有圣性，不保留灵性）
```

### C. SwordForgeUI.DoForgeSword 路径A (SwordForgeUI.lua:712-729)

```
行为：原地变换 weapon 字段，保留 forgeStat，spiritStat = newItem.spiritStat (= nil)
结论：✅ 正确（配合 B 的设计目标）
```

### D. ForgeRandomSetLingqi (LootSystem.lua:1621)

```
行为：遍历 EquipmentData.SetBonuses 全部 8 个 key
BUG：应只从 4 个制式套装中随机
修复：改为读取白名单 FORGE_SET_POOLS.lingqi_t9_standard
```

### E. CreateLonghunling (LootSystem.lua:901)

```
行为：调用 CreateFabaoEquipment("fabao_longhunling", 10, "red")，不注入 saintStat
结论：✅ 正确
```
