# 处罚记录：UID 1364895230（指上人间）

> 记录时间：2026-06-01

---

## 玩家信息

| 字段 | 值 |
|------|-----|
| TapTap UID | 1364895230 |
| TapTap 昵称 | 指上人间 |
| 仙石数量 | 154,434（异常） |
| 仙石榜排名 | #1 |
| 处罚原因 | 仙石数据异常，疑似作弊 |
| 角色信息 | 未获取到（玩家未在新版本上线后登录） |

---

## 已实施的代码措施

### 1. 仙石扣除 + 全部排行榜清零（服务端，登录时触发）

**文件**：`scripts/network/SlotReadService.lua:238`

```lua
local BAN_TARGET_UID = 1364895230
if BAN_TARGET_UID and userId == BAN_TARGET_UID then
    -- 幂等标志：admin_op_ban_zhishang_renjian
    -- 扣除全部仙石余额（MoneyCost）
    -- 清除排行榜分数：
    --   xianshi_rank = 0
    --   trial_floor = 0
    --   prison_floor = 0
    --   rank2_score_1~5 = 0
    --   rank2_kills_1~5 = 0
    --   rank2_time_1~5 = 0
end
```

**触发条件**：该 UID 玩家登录并触发 `HandleFetchSlots` 时自动执行一次。  
**幂等保护**：执行后写入标志位 `admin_op_ban_zhishang_renjian = 1`，不会重复扣除。

### 2. 角色信息打印（服务端，登录时触发）

**文件**：`scripts/network/SlotReadService.lua:289`

```lua
if userId == 1364895230 then
    -- 打印 [ADMIN-CHARINFO] 日志
    -- 包含：每个 slot 的角色名、等级、境界、职业、章节
    -- 以及详细数据：金币、仙石、战力、游玩时间
end
```

**触发条件**：同上，登录时触发。  
**目的**：获取该账号所有角色信息，为后续处罚提供依据。

### 3. 仙石榜 UI 显示 UID（客户端，永久）

**文件**：`scripts/ui/XianshiRankUI.lua`

- 面板宽度 500→580
- 每条排行显示两行：昵称 + UID（小号灰色字体，无前缀）
- 方便后续直接通过榜单获取可疑玩家 UID

---

## 未实施的措施

| 措施 | 状态 | 说明 |
|------|------|------|
| 加入 `Blacklist.userIds` | ❌ 未执行 | 用户明确说"暂不封号" |
| 加入 `Blacklist.kickOnLogin` | ❌ 未执行 | 用户明确说"暂不封号" |
| 客户端排行榜过滤 | ❌ 未执行 | 需加入 `Blacklist.userIds` 才生效 |

---

## 当前状态与问题

**问题**：截至 2026-06-01 12:39，该玩家仍然显示在仙石榜上，扣除操作未生效。

**原因分析**：

扣除代码的触发机制是**玩家登录时服务端执行**。如果该玩家自代码发布后未登录，则操作不会触发。当前无法从开发环境确认生产服运行日志。

**可选的立即生效方案**（未实施）：

1. 将 `1364895230` 加入 `Blacklist.userIds`，`FilterRankList` 会在客户端直接过滤隐藏该玩家（不依赖对方登录）
2. 等待玩家登录，服务端代码自动执行扣除+清榜

---

## 相关文件清单

| 文件 | 修改内容 |
|------|---------|
| `scripts/network/SlotReadService.lua` | BAN_TARGET_UID=1364895230 扣仙石清榜 + 角色信息打印 |
| `scripts/ui/XianshiRankUI.lua` | 面板拉宽 + UID 双行显示 |
| `scripts/network/RankHandler.lua` | 移除了临时 ADMIN-TRACE 日志 |
| `scripts/config/Blacklist.lua` | 未修改（暂不封号） |
| `scripts/server_main.lua` | 未修改（暂不踢登录） |
